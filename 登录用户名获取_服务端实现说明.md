# 用户名查询接口 — 服务端实现说明

> 对应需求：`登录用户名获取_服务端需求.md`（v1，2026-08-01）
> 落地日期：2026-08-02
> 适用模块：聊天服务器（`47.107.33.154:8085`）

本说明配套客户端文档 `登录用户名获取_服务端需求.md` 一起看，目的是说明：
1. 服务端具体怎么实现的；
2. 与需求文档对比，哪些一致、哪些妥协、哪些暂未做；
3. 客户端对接时要注意的点。

---

## 1. 落地清单 vs. 需求文档

| 需求章节 | 状态 | 备注 |
|---|---|---|
| §2 接口 A：`POST /api/v1/auth/login` 扩展 `user_name` 字段 | ✅ 已实现 | 三方模式 + 内部账号模式都已支持 |
| §2.3 显示名取值优先级（client > biz > user_id 兜底） | ✅ 已实现 | 三方模式 + 内部模式都按此优先级 |
| §2.5 错误响应（400/401） | ✅ 已实现 | 长度超限 / 含控制字符 → 400 |
| §2.6 JWT `name` claim | ✅ 已实现 | `auth.py` `issue_token` 新增 `name` 参数 |
| §2.7 落库与更新策略 | ✅ 已实现 | client 带 user_name 且与库不同 → 更新 `users.username` |
| §3 接口 B：`GET /api/v1/users/{user_id}/name` | ✅ 已实现 | 含鉴权、格式校验、404 |
| §3.6 批量查询 `POST /api/v1/users/names` | ✅ 已实现 | ≤100，不存在省略 |
| §3.7 缓存 | ⚠️ 部分实现 | 进程内 TTL 600s 占位，**未接 Redis**（详见 §3） |
| §5 WS 事件影响（knock / speaking / member_joined / owner_*） | ✅ 已实现 | 详见 §4 |

---

## 2. 接口契约（最终版）

### 2.1 `POST /api/v1/auth/login`（扩展）

请求体（与原版相比**新增** `user_name`）：

```json
{
  "external_token": "<biz_token>",
  "user_name": "客户端传入的用户显示名（可选，推荐必填）",
  "username": "可选，内部账号模式用户名",
  "password": "可选，内部账号模式密码"
}
```

成功响应（与原版相比**新增** `name` 字段）：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "username": "alice",          // 与库中一致（已更新的最终值）
    "name": "AliceDisplayName",   // 实际生效的"显示名"（与 username 同语义；冗余便于客户端核对）
    "user_id": "user_abc123456789",
    "room_id": null,
    "role": "member",
    "token": "eyJhbGc...",
    "expires_at": 1785306254,
    "expires_in": 604800
  }
}
```

错误响应（在原基础上**新增**）：

| HTTP | code | message | 触发条件 |
|------|------|---------|----------|
| 400 | 400 | `user_name 长度超过 64` | `user_name` 长度 > 64 |
| 400 | 400 | `user_name 格式非法` | `user_name` 含控制字符 |
| 400 | 400 | `user_name 长度超过 64` | 内部账号模式下同样校验 |

> 与需求文档差异：未单独区分 401/原 401 错误响应（保留原 `token 无效或已过期` 文案）。

---

### 2.2 JWT Claims（扩展）

```json
{
  "sub": "alice",
  "uid": "user_abc123456789",
  "name": "AliceDisplayName",   // 新增
  "room": "...",
  "role": "member",
  "jti": "...",
  "iat": 1785571449,
  "exp": 1786176249,
  "iss": "srs-project"
}
```

> 旧 token（无 `name` 字段）：中间件会 fallback 到 `sub`（即 `username`），向后兼容。
> 客户端**无需重新登录**就能拿到新字段——下次任何 401 触发的刷新时新 token 就会带上。

---

### 2.3 `GET /api/v1/users/{user_id}/name`（新增）

路径：

```
GET /api/v1/users/{user_id}/name
Authorization: Bearer <chat_token>
```

成功响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "user_id": "user_abc123456789",
    "username": "AliceDisplayName",
    "avatar": null
  }
}
```

错误响应：

| HTTP | code | message | 触发条件 |
|------|------|---------|----------|
| 401 | 401 | `token 无效或已过期` | 缺 / 错 token |
| 404 | 404 | `user not found` | `user_id` 在库中不存在 |
| 400 | 400 | `user_id 格式非法` | 不匹配 `^user_[A-Za-z0-9]{12}$` |

---

### 2.4 `POST /api/v1/users/names`（新增，批量）

请求：

```json
{
  "user_ids": ["user_abc123456789", "user_def456789012", "user_xxxinvalidxxxxx"]
}
```

成功响应（不存在的 `user_id` 静默省略；非法 `user_id` 也省略不报错）：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "users": [
      {"user_id": "user_abc123456789", "username": "Alice", "avatar": null},
      {"user_id": "user_def456789012", "username": "Bob",   "avatar": null}
    ]
  }
}
```

错误响应：

| HTTP | code | message | 触发条件 |
|------|------|---------|----------|
| 401 | 401 | `token 无效或已过期` | 缺 / 错 token |
| 400 | 400 | `user_ids 长度不能超过 100` | 数组长度 > 100 |

---

## 3. 缓存说明（与需求文档 §3.7 的妥协）

需求文档建议用 Redis（`chat:user:name:<user_id>`，TTL 600s）。

**本次实现**：进程内 `dict` + `threading.RLock` + TTL 时间戳占位（`user_name_resolver.py`）。

理由：
- 单实例部署（当前 SRS callback server 为单进程）；
- 零依赖、零运维成本；
- 接口（`resolve_display_name`）与 Redis 实现可平滑替换——后续多实例部署时换成 Redis
  只需替换该模块的内部实现，调用方不变。

行为细节：
- TTL 600s；
- `/auth/login` 触发了 `username` 更新时，同步调用 `invalidate_user_name_cache(user_id)`；
- 缓存击穿保护：兜底值（即 `user_id`）也会被写入缓存，避免反复落 DB。

后续若升级 Redis：
- key 形如 `chat:user:name:<user_id>`；
- TTL 600s；
- `/auth/login` 更新 username 时 `DEL` 该 key；
- 接口 B 单 IP 限频 60 req/min（建议用 nginx / API 网关实现）。

---

## 4. WebSocket 事件影响

| 事件 | 字段 | 实现位置 |
|------|------|----------|
| `room_knock` | `data.name` | `server_fastapi.py` `knock_door()` |
| `user_speaking_start` | `data.user_name` | `broadcast_speaking_event()` |
| `user_speaking_stop` | `data.user_name` | `broadcast_speaking_event()` |
| `member_joined` | `data.name` | `join_room()` |
| `owner_online` | `data.owner_name` | `join_room()`（房主自己重连时） |
| `owner_offline` | `data.owner_name` | `leave_room()`（房主离开时） |

取名优先级：
1. 当前请求的 `request.state.name`（JWT `name` claim + 中间件实时用 user_store 校正）；
2. 兜底：被叫方自己的 `user_id`（如 `owner_offline` 房主不在请求上下文里）。

---

## 5. 代码改动清单

| 文件 | 改动 |
|------|------|
| `auth.py` | `UserStore.get_by_user_id()`、`UserStore.update_username()`；`issue_token()` 增加 `name` 参数写入 JWT |
| `user_name_resolver.py` | **新增**：user_name 校验 + 进程内 TTL 缓存 + `resolve_display_name()` |
| `server_fastapi.py` | `LoginRequest.user_name`；`auth_login()` 优先级逻辑 + 落库；中间件注入 `request.state.name`；新增 `GET /users/{user_id}/name` + `POST /users/names`；4 个 WS 事件携带 name |

---

## 6. 客户端对接提示（来自需求文档 §9）

| 文件 | 改动 |
|------|------|
| `module_chat/.../ChatApi.kt` | `AuthApi.USER_NAME = "/api/v1/users/{user_id}/name"`、`LoginRequest.user_name` |
| `module_chat/.../RoomTool.kt` 或 `UserTool.kt` | `getUserName(client, userId)` |
| `module_chatroom/.../ChatManager.kt` | 登录前透传 `LoginUtil.userName` 给聊天服务器 |
| `module_chatroom/.../ChatPrefs.kt` | 缓存当前 `chatUserName`，避免每次进房间都查 |

---

## 7. 验收对照

| 用例 | 期望 | 实现 |
|------|------|------|
| 8.1 登录带 user_name | `data.username == "NuoLordGod"`，`/auth/me` 一致 | ✅ |
| 8.2 登录不带 user_name | 取业务后端 nickname | ✅（向下兼容） |
| 8.3 按 user_id 查名 | 200 + username | ✅ |
| 8.4 按 user_id 查名（未鉴权） | 401 | ✅ |
| 8.5 按 user_id 查名（不存在） | 404 | ✅ |
| 8.6 WS 事件回显新 user_name | knock data.name 是显示名 | ✅ |

---

## 8. 未做事项

| 事项 | 原因 |
|------|------|
| Redis 缓存 | 用进程内占位，多实例部署时再升级 |
| 接口 B 单 IP 限频 60 req/min | 留给 nginx / API 网关层 |
| 头像 `avatar` 字段 | `users` 表无列，固定返回 `null`（需求 §11 已说明） |
| 改名实时广播 | 需求 §11 已说明下次登录时刷新即可 |