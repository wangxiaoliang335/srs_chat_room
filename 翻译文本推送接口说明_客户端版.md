# 翻译文本推送接口说明（客户端版）

## 概述

翻译文本通过 **WebSocket 协议** 实时推送给客户端。客户端需要连接翻译 WebSocket 服务来接收翻译结果。

## 连接方式

### WebSocket 连接地址

```
ws://<服务器地址>:8086/ws?user_id=<用户ID>&room=<房间ID>
```

### 连接示例

```javascript
// 用户 ID: 3 进入房间 room_1779076465986
const ws = new WebSocket('ws://47.107.33.154:8086/ws?user_id=3&room=room_1779076465986');

ws.onopen = () => {
    console.log('WebSocket 已连接');
};

ws.onmessage = (event) => {
    const data = JSON.parse(event.data);
    handleMessage(data);
};
```

## 消息格式

### 翻译文本消息 (translation_text)

当翻译服务完成一段语音的翻译后，会通过 WebSocket 推送翻译文本消息。

**消息类型**: `translation_text`

**消息结构**:

```json
{
    "event_type": "translation_text",
    "event_id": "a1b2c3d4",
    "room_id": "room_1779076465986",
    "user_id": "71",
    "target_user_id": "3",
    "data": {
        "original_text": "你好",
        "translated_text": "Hello",
        "source_lang": "zh",
        "target_lang": "en"
    },
    "timestamp": "2026-05-18 12:22:00"
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| event_type | string | 消息类型，固定为 `translation_text` |
| event_id | string | 事件唯一 ID |
| room_id | string | 房间 ID |
| user_id | string | 说话人用户 ID（源语音用户） |
| target_user_id | string | 目标用户 ID（接收翻译的用户） |
| data.original_text | string | 原始语音识别文本（中文） |
| data.translated_text | string | 翻译后的文本（英文） |
| data.source_lang | string | 源语言代码 |
| data.target_lang | string | 目标语言代码 |
| timestamp | string | 事件时间戳 |

## 客户端接收示例

```javascript
function handleMessage(data) {
    switch (data.event_type) {
        case 'translation_text':
            handleTranslationText(data);
            break;
        case 'connected':
            console.log('服务已连接:', data.message);
            break;
        case 'subscribed':
            console.log('订阅成功');
            break;
        case 'pong':
            // 心跳响应，忽略
            break;
        default:
            console.log('未知消息类型:', data.event_type);
    }
}

function handleTranslationText(data) {
    const { room_id, user_id, data: content } = data;
    const { original_text, translated_text, source_lang, target_lang } = content;

    console.log(`用户 ${user_id} 说: ${original_text}`);
    console.log(`翻译为 ${target_lang}: ${translated_text}`);

    // 在 UI 上显示翻译结果
    showTranslationMessage(user_id, original_text, translated_text);
}
```

## 消息类型汇总

客户端可能收到的 WebSocket 消息类型：

| 消息类型 | 说明 | 处理方式 |
|----------|------|----------|
| `connected` | 连接成功 | 忽略或记录日志 |
| `subscribed` | 订阅成功 | 忽略或记录日志 |
| `translation_text` | 翻译文本 | 显示给用户 |
| `pong` | 心跳响应 | 忽略 |
| `error` | 错误消息 | 记录错误日志 |

## 完整 WebSocket 消息示例

### 1. 连接成功
```json
{
    "type": "connected",
    "message": "Service connected",
    "timestamp": "2026-05-18 12:22:00"
}
```

### 2. 订阅成功
```json
{
    "type": "subscribed",
    "message": "Subscribed to room successfully",
    "room_id": "room_1779076465986"
}
```

### 3. 翻译文本
```json
{
    "event_type": "translation_text",
    "event_id": "a1b2c3d4",
    "room_id": "room_1779076465986",
    "user_id": "71",
    "target_user_id": "3",
    "data": {
        "original_text": "你好",
        "translated_text": "Hello",
        "source_lang": "zh",
        "target_lang": "en"
    },
    "timestamp": "2026-05-18 12:22:00"
}
```

### 4. 心跳响应
```json
{
    "type": "pong"
}
```

## 注意事项

1. **心跳维护**: 建议客户端每 30 秒发送一次心跳 `{"type": "ping"}`，服务器会回复 `{"type": "pong"}`

2. **连接断开重连**: WebSocket 断开后，客户端应自动尝试重连

3. **消息解析**: 所有消息都是 JSON 格式，请确保正确解析

4. **语言代码**:
   - `zh` - 中文
   - `en` - 英文
   - `ja` - 日文
   - `ko` - 韩文
   - `fr` - 法文
   - `de` - 德文
   - 等等...

## 错误处理

```javascript
ws.onerror = (error) => {
    console.error('WebSocket 错误:', error);
};

ws.onclose = (event) => {
    console.log('WebSocket 连接关闭:', event.code, event.reason);
    // 自动重连逻辑
    setTimeout(() => {
        reconnect();
    }, 3000);
};
```
