# 聊天服务器登录与用户名称查询接口需求

> 适用模块：聊天服务器（srs-project，`47.107.33.154:8085`）
> 客户端模块：`module_chat`（AAR）/ `module_chatroom`（业务接入）
> 编写日期：2026-08-01

---

## 1. 背景与目标

当前 `/api/v1/auth/login` 返回体里已含 `username`，但 `username` 由业务后端
`getCurrentProfile` 接口提供。在以下场景存在问题：

1. **业务后端 `getCurrentProfile` 不返回 `nickname/username` 时，聊天服务器拿不到显示名**
   - 例：业务后端只返回 `id`，聊天模块只能回退到 `user_id` 当作显示名，体验差。
2. **聊天室内（房间内列表、敲门请求、消息列表、说话状态广播）需要的"用户显示名"和
   聊天服务器登录态没有绑定**
   - 客户端敲门时只能把 `user_id` 当作 `knockerName` 发出去。
   - 房主看到的敲门请求列表显示的是 `user_xxx` 而不是真实昵称。
3. **房间内成员列表、消息发送者名称需要被其他客户端实时查询**
   - 当前缺乏一个统一的"按 user_id 取用户名"的接口，前端各自缓存容易不一致。

为此提出两个改动：

| # | 接口 | 说明 |
|---|------|------|
| A | `POST /api/v1/auth/login` | 请求体新增可选字段 `user_name`，登录时由客户端显式带入，作为聊天室内的用户显示名 |
| B | `GET /api/v1/users/{user_id}/name`（**新增**） | 通过用户 ID 查询用户名，需 `chat_token` 鉴权 |

---

## 2. 接口 A：登录接口扩展

### 2.1 路径

```
POST /api/v1/auth/login
```

公开接口，无需 `Authorization` Header。

### 2.2 请求体

```json
{
  "external_token": "业务后端返回的 token",
  "user_name": "客户端传入的用户显示名（可选，推荐必填）",
  "username": "可选，备用用户名",
  "password": "可选，备用密码"
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| external_token | string | 二选一 | 业务后端 token，登录鉴权用 |
| username | string | 二选一 | 内部账号模式用户名 |
| password | string | 内部账号模式必填 | 内部账号模式密码 |
| **user_name** | **string** | **否（推荐必填）** | **客户端期望写入聊天服务器的用户显示名。1 ≤ length ≤ 64，去除首尾空白后非空。** |

### 2.3 取值优先级（聊天服务器存入的 username）

服务器拿到用户显示名时，按以下顺序取值并落库（写入 `users` 表对应字段）：

1. 请求体显式传入的 `user_name`（**本次新增，最高优先级**）
2. 业务后端 `getCurrentProfile` 响应里的 `nickname` / `username` / `avatar` 字段（兼容旧逻辑）
3. 兜底：`user_<12位>` 形式的 `user_id`（即原默认行为，仅在前两者都拿不到时使用）

> 说明：客户端在 `LoginUtil.userName` 已知时**必须**显式传 `user_name`，
> 这样即使业务后端 `getCurrentProfile` 因网关问题不返回昵称，聊天室内的"
> 谁在说话/谁敲门/谁发消息"显示名仍正确。

### 2.4 成功响应（不变）

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "username": "alice",
    "user_id": "user_abc123456789",
    "room_id": null,
    "role": "member",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expires_at": 1785306254,
    "expires_in": 604800
  }
}
```

- `username` 字段：返回最终生效的显示名（即按 2.3 优先级取到的结果），供客户端比对回写。

### 2.5 错误响应（在原有基础上追加）

| HTTP | code | message | 说明 |
|------|------|---------|------|
| 400 | 400 | user_name 长度超过 64 | `user_name` 长度超限 |
| 400 | 400 | user_name 格式非法 | `user_name` 含控制字符或仅空白 |
| 401 | 401 | token 无效或已过期 | 业务后端验证失败（保留原行为） |

### 2.6 JWT Claims 扩展（**强烈建议**）

为了让"按 user_id 取用户名"的接口以及 WebSocket 推送事件能直接从 JWT 解析显示名
（避免每次都查库），JWT Payload 增加字段：

```json
{
  "sub": "Nuo",
  "uid": "user_50ebf1752dbf",
  "name": "NuoLordGod",        // 新增：客户端传入的 user_name
  "room": "...",
  "role": "member",
  "jti": "...",
  "iat": 1785571449,
  "exp": 1786176249,
  "iss": "srs-project"
}
```

后端所有需要显示用户名的地方（WebSocket 事件 `room_knock`、`speaking_started`、
消息推送 `sender_name` 等）优先取 JWT 中的 `name` 字段。

### 2.7 落库与更新策略

- 首次登录：插入 `users` 表，`username` 字段按 2.3 优先级取值。
- 重复登录：
  - 若请求体带了 `user_name` 且与库中 `username` 不同 → **更新库中 `username`**
  - 若未带 `user_name` → 保留库中 `username` 不变（向后兼容旧客户端）
- `users` 表已有 `username` 列，本需求无需新增表结构。

---

## 3. 接口 B：按 user_id 查询用户名

### 3.1 路径

```
GET /api/v1/users/{user_id}/name
```

- 路径参数 `user_id`：格式 `user_<12位字符>`，例如 `user_50ebf1752dbf`

### 3.2 鉴权

需在 Header 携带聊天服务器 JWT：

```
Authorization: Bearer <chat_token>
```

- token 无效 / 过期 → 401（与现有所有鉴权接口一致）
- token 有效但请求的 `user_id` 不存在 → 404
- token 有效且 `user_id` 存在但库中无 username → 200，`username` 回退到 `user_id`

### 3.3 请求示例

```http
GET /api/v1/users/user_50ebf1752dbf/name
Host: 47.107.33.154:8085
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 3.4 成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "user_id": "user_50ebf1752dbf",
    "username": "NuoLordGod",
    "avatar": null
  }
}
```

字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | string | 原样回传，便于客户端核对 |
| username | string | 用户显示名。若库中没有则回退到 `user_id` 字符串本身 |
| avatar | string/null | 头像 URL，**可选字段**，聊天服务器 `users` 表中无该列时固定返回 `null`（保留扩展位） |

### 3.5 错误响应

| HTTP | code | message | 说明 |
|------|------|---------|------|
| 401 | 401 | token 无效或已过期 | 未带/无效 token |
| 404 | 404 | user not found | path 中的 `user_id` 在 `users` 表中不存在 |
| 400 | 400 | user_id 格式非法 | 不匹配 `^user_[A-Za-z0-9]{12}$` |

### 3.6 批量查询（可选扩展，强烈建议同时实现）

为降低房间内成员列表渲染时的请求次数，建议同时实现批量接口：

```
POST /api/v1/users/names
Authorization: Bearer <chat_token>
```

请求体：

```json
{
  "user_ids": ["user_50ebf1752dbf", "user_abc123456789", "user_ffffffffffff"]
}
```

- `user_ids` 数组长度 ≤ 100，超过返回 400。
- 数组中不存在的 `user_id` 直接在响应中省略，不抛错。

成功响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "users": [
      { "user_id": "user_50ebf1752dbf", "username": "NuoLordGod", "avatar": null },
      { "user_id": "user_abc123456789", "username": "alice",      "avatar": null }
    ]
  }
}
```

未认证 / token 失效同 3.5。

### 3.7 缓存与一致性

- 服务器侧：建议给 `users.username` 加 Redis 缓存，key `chat:user:name:<user_id>`，TTL 600s。
  - `POST /auth/login` 时若触发了 `username` 更新（2.7），同步刷新/删除该缓存。
- 客户端侧：拿到响应后可缓存到本地（已计划在 `ChatPrefs` 中保存当前登录用户名）。

---

## 4. 数据库改动

`users` 表已有 `username` 字段，**本次不新增表，不新增列**。

唯一一处索引建议（性能优化，非必须）：

```sql
-- 若 users.id 已是主键，则 user_id 已有索引；username 查询频率不高，无需额外索引
-- 仅在批量接口上线时考虑给 username 建普通索引
CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_users_username ON users(username);
```

---

## 5. WebSocket 事件影响

事件载荷中所有"用户名"字段应统一改为取 **JWT `name` claim → DB `users.username`** 的优先级：

| 事件 type | 字段 | 现状 | 改动 |
|-----------|------|------|------|
| `room_knock` | `name` | 从业务后端取，常为空 → 客户端回退 `user_id` | 优先取 JWT `name`（客户端登录时传入的 `user_name`） |
| `speaking_started` / `speaking_stopped` | `user_name` | 同上 | 同上 |
| `room_member_joined` | `name` | 同上 | 同上 |
| `message_send`（推送）| `sender_name` | 同上 | 同上 |

> 目的：让聊天室内"谁敲门 / 谁在说话 / 谁加入 / 谁发消息"显示的名字最终都指回客户端登录时传入的 `user_name`。

---

## 6. 鉴权与安全

1. 接口 A（登录）保持公开，不做 IP 限频以外的鉴权。
2. 接口 B（按 id 查名）必须验证 `chat_token`：
   - JWT 签名校验 + `exp` 校验 + `iss == "srs-project"` 校验。
   - 任意身份的合法 token 都能查询任意 `user_id` 的名字（不限制只能查自己）—— 聊天室场景默认成员互相可见。
3. 接口 B 单 IP 限频建议：60 req/min（防扫描）。

---

## 7. 向后兼容

- 旧客户端不传 `user_name`：服务器走原 fallback 链路，行为与现状一致。
- 旧客户端不知道接口 B：不影响，是新增接口。
- 现有所有携带 `Authorization: Bearer <token>` 的接口**不破坏**。

---

## 8. 验收用例

### 8.1 登录带 user_name

```http
POST /api/v1/auth/login
Content-Type: application/json

{
  "external_token": "<biz_token>",
  "user_name": "NuoLordGod"
}
```

期望响应 `data.username == "NuoLordGod"`，且再次 `GET /api/v1/auth/me` 时
`username` 字段一致。

### 8.2 登录不带 user_name（兼容）

```http
POST /api/v1/auth/login
{ "external_token": "<biz_token>" }
```

期望响应 `data.username` 仍取业务后端返回的 nickname，不报错。

### 8.3 按 user_id 查名（正常）

```http
GET /api/v1/users/user_50ebf1752dbf/name
Authorization: Bearer <chat_token>
```

期望 `200`，`data.username == "NuoLordGod"`。

### 8.4 按 user_id 查名（未鉴权）

```http
GET /api/v1/users/user_50ebf1752dbf/name
```

期望 `401`，`code == 401`，`message == "token 无效或已过期"`。

### 8.5 按 user_id 查名（不存在）

```http
GET /api/v1/users/user_zzzzzzzzzzzz/name
Authorization: Bearer <chat_token>
```

期望 `404`，`message == "user not found"`。

### 8.6 WebSocket 事件回显新 user_name

A 用户带 `user_name = "Nuo"` 登录 → 进房间 → 房主收到 `room_knock` 时
`data.name == "Nuo"`（而非 `user_xxx`）。

---

## 9. 客户端配合改动（已就绪/待联调）

| 文件 | 改动 |
|------|------|
| `module_chat/.../ChatApi.kt` | 新增 `AuthApi.USER_NAME = "/api/v1/users/{user_id}/name"`、`LoginRequest.user_name` |
| `module_chat/.../RoomTool.kt` 或新增 `UserTool.kt` | `getUserName(client, userId)` |
| `module_chatroom/.../ChatManager.kt` | 登录前透传 `LoginUtil.userName` 给聊天服务器 |
| `module_chatroom/.../ChatPrefs.kt` | 缓存当前 `chatUserName`，避免每次进房间都查 |

---

## 10. 上线步骤

1. 服务端实现接口 A 扩展并部署到测试环境。
2. 服务端实现接口 B 并经 Postman 跑通 8.3/8.4/8.5。
3. 客户端发新的 `module_chat-release.aar`。
4. 客户端走通 8.1/8.2/8.6。
5. 灰度上线，观察 `users.username` 是否按预期更新。
6. 全量上线。

---

## 11. 未尽事项

- 用户头像 `avatar`：聊天服务器目前 `users` 表无此列，本次接口先返回 `null`。后续若需要展示头像，需要业务后端 `getCurrentProfile` 透传 avatar 并由聊天服务器落库，再让本接口返回。
- 用户改名通知：如果允许用户在业务后端改名，业务后端推送或客户端下次登录时刷新即可，本需求不涉及实时改名广播。