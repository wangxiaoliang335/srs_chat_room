# 房间邀请功能 — 服务端需求文档

## 1. 概述

实现用户之间发送/接收房间邀请的功能。邀请方在房间内选择用户发送邀请，被邀请方实时收到邀请通知，可选择接受或拒绝。

### 涉及文档

- `E:\Project\Chat\docs\服务端文档.md`（现有 API 规范，本需求保持风格一致）
- `E:\Project\Chat\docs\CHAT_MODULE.md`

---

## 2. 数据模型

### 2.1 邀请记录 `Invitation`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一 ID，格式 `inv_<12位随机字符>` |
| room_id | string | 房间 ID |
| room_name | string | 房间名称 |
| inviter_id | string | 邀请者 user_id |
| inviter_name | string | 邀请者用户名 |
| invitee_id | string | 被邀请者 user_id |
| status | string | `pending` / `accepted` / `rejected` / `expired` |
| message | string | 邀请附言（可选） |
| created_at | int | 创建时间戳（Unix 秒） |
| expires_at | int | 过期时间戳（创建后 24 小时） |

---

## 3. HTTP API

基础 URL: `http://{host}:8085`

> 以下接口均需 `Authorization: Bearer <聊天服务器 JWT>`。

### 3.1 发送邀请

```
POST /api/v1/invite
```

**请求体：**

```json
{
  "room_id": "room_xxx",
  "invitee_id": "user_xxx",
  "message": "一起来聊天吧！"
}
```

**逻辑：**
1. 校验邀请者在 `room_id` 房间中（且不是被禁言/禁麦状态）
2. 校验被邀请者未在房间中
3. 校验被邀请者没有待处理的该房间邀请（防止重复发送）
4. 创建邀请记录，状态 `pending`
5. 向被邀请者推送 WebSocket 事件 `room_invite`

**成功响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": "inv_abc123def456"
  }
}
```

**错误码：**

| HTTP 状态码 | code | message | 说明 |
|-------------|------|---------|------|
| 400 | 400 | inviter not in room | 邀请者不在房间 |
| 400 | 400 | invitee already in room | 被邀请者已在房间 |
| 400 | 400 | duplicate invitation | 已有待处理的邀请 |
| 404 | 404 | room not found | 房间不存在 |

### 3.2 获取待处理的邀请

```
GET /api/v1/invites/pending
```

**说明：** 返回当前用户所有 `status=pending` 的邀请。

**成功响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": "inv_abc123def456",
      "room_id": "room_xxx",
      "room_name": "闲聊室",
      "inviter_id": "user_xxx",
      "inviter_name": "Alice",
      "created_at": 1785306254,
      "message": "一起来聊天吧！"
    }
  ]
}
```

> 不返回 `invitee_id`（查询的就是当前用户）和 `expires_at`（客户端不需要关心过期逻辑）。

### 3.3 接受邀请

```
POST /api/v1/invite/{id}/accept
```

**逻辑：**
1. 校验邀请状态为 `pending`
2. 更新状态为 `accepted`
3. 调已有接口 `POST /api/v1/room/{room_id}/join` 将被邀请者加入房间
4. 向邀请者推送 WebSocket 事件 `room_invite_accepted`

**成功响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "room_id": "room_xxx",
    "user_id": "user_xxx"
  }
}
```

### 3.4 拒绝邀请

```
POST /api/v1/invite/{id}/reject
```

**请求体：**

```json
{
  "reason": "没时间"
}
```

**逻辑：**
1. 校验邀请状态为 `pending`
2. 更新状态为 `rejected`
3. 向邀请者推送 WebSocket 事件 `room_invite_rejected`

**成功响应：**

```json
{
  "code": 0,
  "message": "success"
}
```

### 3.5 批量获取邀请信息（可选）

```
POST /api/v1/invites/batch
```

用于客户端初始化时一次拉取多条邀请详情。

**请求体：**

```json
{
  "ids": ["inv_abc1", "inv_abc2"]
}
```

**成功响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": "inv_abc1",
      "room_id": "room_xxx",
      "room_name": "闲聊室",
      "inviter_id": "user_xxx",
      "inviter_name": "Alice",
      "status": "pending",
      "created_at": 1785306254,
      "message": "一起来聊天吧！"
    }
  ]
}
```

---

## 4. WebSocket 事件

房间通知 WebSocket（端口 8086）新增以下事件类型：

### 4.1 `room_invite` — 收到邀请

推送给被邀请者。

```json
{
  "type": "room_invite",
  "event_id": "string",
  "room_id": "room_xxx",
  "data": {
    "invitation_id": "inv_xxx",
    "inviter_id": "user_xxx",
    "inviter_name": "Alice",
    "room_name": "闲聊室",
    "message": "一起来聊天吧！",
    "created_at": 1785306254
  },
  "timestamp": "2026-07-27T10:00:00Z"
}
```

### 4.2 `room_invite_accepted` — 邀请被接受

推送给邀请者。

```json
{
  "type": "room_invite_accepted",
  "event_id": "string",
  "room_id": "room_xxx",
  "data": {
    "invitation_id": "inv_xxx",
    "invitee_id": "user_xxx",
    "invitee_name": "Bob"
  },
  "timestamp": "2026-07-27T10:00:00Z"
}
```

### 4.3 `room_invite_rejected` — 邀请被拒绝

推送给邀请者。

```json
{
  "type": "room_invite_rejected",
  "event_id": "string",
  "room_id": "room_xxx",
  "data": {
    "invitation_id": "inv_xxx",
    "invitee_id": "user_xxx",
    "invitee_name": "Bob",
    "reason": "没时间"
  },
  "timestamp": "2026-07-27T10:00:00Z"
}
```

---

## 5. 事件到期清理

服务端定期扫描 `status=pending` 且 `expires_at < now` 的邀请记录，自动标记为 `expired`（建议每小时执行一次）。

---

## 6. 客户端映射

服务端 WebSocket 事件 → 客户端 `ChatEvent` 映射：

```
room_invite            → NotificationEvent.InvitationReceived
room_invite_accepted    → NotificationEvent.InvitationAccepted  (新增)
room_invite_rejected    → NotificationEvent.InvitationRejected  (新增)
```

客户端 `Events.kt` 中已有 `NotificationEvent.InvitationReceived(inviterName, roomName)`，需同步新增 `InvitationAccepted` 和 `InvitationRejected`。
