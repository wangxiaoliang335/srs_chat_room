# 聊天服务器服务端需求文档

> 本文档从**服务端实现视角**描述，供聊天服务器开发使用
> 业务 API 端口 8085，SRS 流媒体端口 1985，翻译 WebSocket 8086
> 变更记录：2026-08-13 优化修订（成员状态改为「在线/离线」，移除「离开」概念；补充幂等、限流、存储分层等；
> 「每个账号只能有 1 个房间」明确为「只能作为 1 个房间的房主、房主不可转让」，成员身份可多房间并存）

---

## 1. 架构总览

### 1.1 服务划分

| 服务 | 端口 | 作用 | 职责边界 | 文档代号 |
|---|---|---|---|---|
| 业务 API + room_socket | 8085 | 房间内事件沟通 | 不处理跨房间类事件 | room_socket |
| notice_socket | 独立端口（custom） | 跨房间事件沟通 | 处理跨房间等消息通知 | notice_socket |

### 1.2 服务职责边界（强制约束）

- `room_socket`：**仅**处理房间内事件（聊天消息、成员在线状态、踢人/禁言/禁麦、说话状态等）。
- `notice_socket`：**仅**处理跨房间事件（邀请、敲门、通知消息推送）。房间之间不通过
  `room_socket` 相互通信。
- 所有跨房间交互（敲门、邀请、通知）必须经 `notice_socket` 抵达对方，不得绕过。

### 1.3 总体流程

```
客户端 ── room_socket(8085, 房间内) ──▶ 聊天服务器
   │
   └── notice_socket(独立端口, 跨房间) ──▶ 聊天服务器 → 目标用户 notice_socket
```

---

## 2. 成员「在线/离线」状态管理

> **核心决策：放弃「离开房间」状态。** 成员关系只区分**在线/离线**，不存在「离开」态。
> 离线成员**仍保留房间关联**（占用房间名额）；是否释放房间由房主关闭/踢人/房间自动清理决定（见第 7 节）。

### 2.1 需求描述

- 服务端通过 `room_socket` 与客户端保持心跳。
- **连续 3 次 ping 未收到 pong** → 判定客户端离线。

### 2.2 服务端职责

1. 维护 `user_id → room_socket 连接` 的映射表（在线状态优先存 Redis，见 2.5）。
2. 心跳机制（参数可配置）：
   - 服务端按固定间隔（默认 30s）向客户端发送 `ping`。
   - 收到 `pong` 则重置失败计数；未收到则计数 +1。
   - 计数达到 3 次 → 标记该用户离线（`online_status = offline`），记录 `offline_at`。
   - 心跳参数（interval、失败阈值）需支持配置化，便于按网络环境调优。
3. 客户端**重新建立** `room_socket` 连接 → 标记在线（`online_status = online`），清空 `offline_at`。
4. 在线状态变更时向房间内其他成员推送 `member_online_status_changed` 事件。
5. 记录用户**最近一次离线时间** `offline_at`；再次上线时清空。
6. **离线不解除房间关联**（见本节顶部决策），因此不需要「离开房间」的触发与清理逻辑。

### 2.3 变更说明

> ⚠️ **移除 `/api/v1/rooms/{room_id}/leave` 接口；`/api/v1/rooms/{room_id}/join`
> 不再设置在线状态。** 在线/离线一律由 `room_socket` 心跳判定。

### 2.4 数据模型

```
MemberPresence:
  user_id        String    聊天用户 id
  room_id        String    所在房间
  online_status  String    online / offline
  offline_at     Long?     最近离线时间戳（null = 在线）
  last_active    Long      最近活跃时间戳
```

### 2.5 存储分层

- **Redis**：在线状态、在线人数、连接映射（高并发读写的热数据，TTL 兜底防脏数据）。
- **MySQL**：`offline_at` / `last_active` 等落库（最终一致，用于查询与审计）。
- 状态变更：先写 Redis，再异步落库；断连时以 Redis 为准恢复现场。

---

## 3. 房间「邀请/敲门」及「接收邀请/接收敲门」

### 3.1 需求描述

- 服务端提供 `notice_socket` 用于接收/推送通知等互动，支持后续扩展。
- 敲门、邀请等跨房间交互**都必须**经过 `notice_socket` 抵达对方。
- 房主**接受邀请**时需生成一个**不重复的邀请码**：
  - 一次性使用，使用后自动失效。
  - 服务端统一管理邀请码，确保唯一性。
  - 有效期默认 **10 分钟**。
- 对房间访问进行安全控制：
  - 仅持有效邀请码才能加入房间。
  - 房主可以拒绝加入房间。

### 3.2 服务端职责

#### 3.2.1 邀请流程（服务端）

1. 用户 A 通过 `notice_socket` 发送 `invite_request` → 服务端校验（含**限流**，见 3.4）→ 推送 `invite_received` 给用户 B。
2. 用户 B 接受邀请（`invite_accept`）：
   - 成员身份可多房间并存（见第 7 节），无需「是否已在房间」校验；若 B 已是某房间房主，其房主身份不受影响。
   - 生成**唯一邀请码**（8~12 位随机串），有效期 10 分钟，状态 `unused`。
   - 返回 `{ room_id, user_id, invite_code, expires_at }` 给 B。
   - 通过 `notice_socket` 推送 `invite_accepted`（携带邀请码）给 A。
3. 用户 B 拒绝邀请（`invite_reject`）→ 推送 `invite_rejected` 给 A。

#### 3.2.2 敲门流程（服务端）

1. 用户 C 通过 `notice_socket` 发送 `knock_request`（含 room_id、留言）→ 服务端校验房间存在（含**限流**，见 3.4）→ 推送 `knock_received` 给房主。
2. 房主接受敲门（`knock_accept`）→ 服务端生成邀请码并推送 `knock_accepted`（携带邀请码）给 C。
3. 房主拒绝敲门（`knock_reject`）→ 推送 `knock_rejected` 给 C。

#### 3.2.3 邀请码管理

| 操作 | 说明 |
|---|---|
| 生成 | 接受邀请/接受敲门时自动生成；房主亦可手动生成（`/api/v1/invite/code/generate`） |
| 校验 | 加入房间时校验（见第 4 节） |
| 使用 | **一次性，CAS 原子消费**（见 3.3），校验通过即标记 `used` |
| 撤销 | 房主可主动撤销（`/api/v1/invite/code/revoke`） |
| 过期 | 定时任务将超期未用码标记 `expired` |

邀请码状态机：

```
unused ──使用──▶ used        （加入房间校验通过时）
unused ──过期──▶ expired     （超 10 分钟）
unused ──撤销──▶ revoked     （房主撤销）
```

### 3.3 并发安全（幂等）

- **邀请码原子消费**：使用 `UPDATE invite_codes SET status='used', used_by=?, used_at=? WHERE code=? AND status='unused'`，
  判断影响行数，**== 1 才视为消费成功**，防止并发下同一码被重复使用。
- 有效邀请码缓存于 Redis（key=`invite:valid:{code}`，TTL=有效期），快速预校验；正式消费以 DB CAS 为准。
- 校验失败连续 N 次（如 5 次/码）→ 该码临时锁定（防止爆破）。

### 3.4 限流防刷

| 动作 | 限制（默认，可配置） |
|---|---|
| 发送邀请 | 每用户 1 次 / 30s；超限返回 `429` |
| 敲门 | 每用户 1 次 / 30s，且同一房间每用户最多 3 次/小时 |
| 邀请码校验 | 失败 5 次/码 → 锁定 5 分钟 |

### 3.5 数据模型

```
InviteCode:
  code            String    邀请码（全局唯一）
  room_id         String    所属房间
  created_by      String    生成者（房主 user_id）
  target_user_id  String?   指定对象（null = 通用邀请码）
  status          String    unused / used / expired / revoked
  used_by         String?   使用者
  created_at      Long
  expires_at      Long      默认 now + 600s
  used_at         Long?

InviteRecord:
  id              String
  from_user_id    String
  to_user_id      String
  room_id         String
  status          pending / accepted / rejected
  invite_code     String?   接受后回填
  created_at      Long

KnockRecord:
  id              String
  user_id         String    敲门人
  room_id         String
  message         String?   敲门留言
  status          pending / accepted / rejected
  invite_code     String?   接受后回填
  created_at      Long
```

---

## 4. 成员「加入房间」管理

> 成员状态仅有**在线/离线**（第 2 节），本节点只描述「加入」。
> 已删除「离开房间」相关逻辑与 `POST /api/v1/rooms/{room_id}/leave` 接口。

### 4.1 需求描述

- **加入房间**：需验证房主提供的邀请码是否有效（未过期 + 未被使用 + 未被撤销）。

### 4.2 服务端职责

#### 4.2.1 加入房间（`POST /api/v1/rooms/{room_id}/join`）

1. 前置校验：成员身份可多房间并存（见第 7 节），无需「不在其他房间」校验；重点校验邀请码（见第 2 步）。
2. 校验邀请码（`invite_code`）：
   - 房主/管理员：无需邀请码。
   - 普通成员：必须携带有效邀请码（未过期 + 未使用 + 未撤销 + 归属该房间）。
   - 校验失败 → `code=403, message="invalid or missing invite code"`。
   - 校验通过 → 邀请码立即 **CAS 标记 `used`**（见 3.3）。
3. 关联 `user ↔ room`，返回 `role / publish_allowed`。
4. 建立 `room_socket` 连接后由心跳判定在线（见第 2 节），join 接口**不再设置在线状态**。

### 4.3 数据模型

```
RoomMember:
  user_id         String
  room_id         String
  role            String    owner / admin / member / guest
  status          String    active / removed    成员关系生命周期（removed=被踢/房间关闭）
  online_status   String    online / offline    纯在线状态，由心跳判定
  joined_at       Long
  offline_at      Long?    最近离线时间（null = 在线）
  last_active     Long
```

### 4.4 并发兜底

- 成员可多房间，`room_members` **不做** user 唯一约束；「每账号最多拥有 1 个房间（房主）」
  由 `rooms(owner_id)` 唯一索引在库层面兜底（详见第 9 节）。
- 成员关系与邀请码消费在同一事务内完成，失败回滚。

---

## 5. 聊天功能「发送/接收」

### 5.1 需求描述

- 需支持**文本消息、图片消息、文件消息**等类型。

### 5.2 服务端职责

1. 消息发送接口 `POST /api/v1/messages/send` 支持 `type`：`text` / `image` / `file`。
2. **幂等**：请求携带 `client_msg_id`（发送方生成），服务端按 `(user_id, client_msg_id)` 去重，
   重试不会产生重复消息；首次入库成功后返回服务端消息 id。
3. **顺序保证**：消息落库时分配自增 `seq`（房间内单调递增），广播与历史查询均按 `seq` 排序。
4. 图片/文件消息需先上传（`POST /api/v1/messages/upload`，multipart/form-data），
   返回 `file_id / url`，消息内容记录 URL 或 file_id。
   - **上传安全**：文件大小上限（如图片 ≤ 10MB、文件 ≤ 100MB）、扩展名/MIME 白名单、
     服务端二次校验（不信任客户端 Content-Type）、病毒扫描、私有存储 + 签名 URL（防越权访问）。
5. 消息持久化，支持历史查询（`GET /api/v1/messages/history`，room_id + limit，按 `seq` 游标分页）。
6. 消息实时分发：通过 `room_socket` 向房间内其他在线成员广播（携带 `seq`）。
7. **离线补拉**：客户端重连后按「本机最后一条 `seq`」增量补拉断线期间的消息。

### 5.3 数据模型

```
Message:
  id             String
  room_id        String
  user_id        String
  username       String?
  client_msg_id  String    发送方幂等 id（(user_id, client_msg_id) 唯一）
  seq            Long      房间内自增序号（顺序保证）
  type           String    text / image / file
  content        String    text=内容；image/file=URL 或 file_id
  file_name      String?   file 消息文件名
  file_size      Long?     file 消息字节数
  mime_type      String?   file/image 消息 MIME
  width          Int?      image 消息宽
  height         Int?      image 消息高
  timestamp      Double    服务端时间戳
```

---

## 6. 通知消息「接收」

### 6.1 需求描述

- 服务端需对通知消息进行记录，包括**通知类型、通知内容、通知时间**等。

### 6.2 服务端职责

1. 通知消息统一持久化，供查询（`GET /api/v1/notifications`，分页、时间倒序）。
2. 通知支持已读/未读管理：
   - 单条已读 `POST /api/v1/notifications/{id}/read`
   - 全部已读 `POST /api/v1/notifications/read-all`
   - 未读数 `GET /api/v1/notifications/unread-count`
   - 删除 `DELETE /api/v1/notifications/{id}`
3. 通知触发时同时通过 `notice_socket` 实时推送给目标用户。
   - **推送可靠性**：推送失败进入失败队列，指数退避重试（如 1s/5s/30s）；目标离线直接跳过推送。
4. **离线重投**：用户离线期间产生的通知只落库；`notice_socket` 重连后按
   `created_at > 最后已读时间` 补推未读通知 + 刷新未读数。
5. 通知类型枚举：

| type | 触发时机 |
|---|---|
| `invite_received` | 收到房间邀请 |
| `invite_accepted` | 你发送的邀请被接受 |
| `invite_rejected` | 你发送的邀请被拒绝 |
| `knock_received` | 有人敲门 |
| `knock_accepted` | 敲门被接受 |
| `knock_rejected` | 敲门被拒绝 |
| `member_joined` | 新成员加入房间 |
| `member_offline` | 成员离线（对应原 member_left，见变更说明） |
| `member_kicked` | 你被踢出房间 |
| `role_updated` | 你的角色被更改 |
| `room_closed` | 房间被关闭 |

> 变更说明：因放弃「离开房间」状态，通知类型由 `member_left`（成员离开房间）改为
> `member_offline`（成员离线）。

### 6.3 数据模型

```
Notification:
  id               String
  user_id          String    接收方
  type             String    见上表枚举
  title            String
  content          String
  room_id          String?
  related_user_id  String?
  data             JSON?     扩展字段（如邀请码、角色）
  is_read          Boolean
  created_at       Long
  read_at          Long?
```

---

## 7. 房间限制「每个账号只能拥有 1 个房间」

### 7.1 需求描述

- **房主身份限制**：每个账号最多只能**拥有（作为房主）1 个房间**。
- **房主不可转让**：房主身份无法转让给其他用户。
- **房主房间生命周期**：房主房间**不会自动消失**，仅以下方式删除：
  - 房主主动关闭（`DELETE /api/v1/rooms/{room_id}`）。
  - 违规被封禁。
- **成员身份不限**：用户（**包括房主**）作为成员可**同时存在于多个房间**，「1 个房间」限制
  **仅针对「拥有」**（房主身份），不限制成员的房间数量。
- **房间与用户关联**：服务端对房间与用户进行关联；若房间无任何用户关联，自动删除（兜底）。

### 7.2 服务端职责

1. **房主唯一性校验（创建/覆盖房间）**：
   - 调用 `POST /api/v1/rooms` 时：
     - 用户非任何房间房主 → 正常创建，用户成为 owner。
     - 用户**已是某房间房主** → 不允许新建新房间，服务端将该请求**覆盖（更新）现有房间**
       （如修改房间名称等，room_id 不变）。
   - 并发兜底：`rooms(owner_id)` 唯一索引 + 事务（见 4.4 / 9.1）。
   - `login` 响应中回显 `room_id`（非 null = 已拥有房间）。
2. **房主不可转让**：
   - 不提供 owner 转让接口，无 owner 变更逻辑；owner 身份不随成员加入/离开/踢人改变，
     仅随房间关闭或违规封禁而终结。
3. **房主房间不自动删除**：
   - 有 owner 的房间（房主即为成员，`member_count >= 1`）**不参与**「空房间自动清理」。
4. **成员多房间并存（含房主）**：
   - 加入他人房间（经邀请码，见第 4 节）**不受数量限制**，可同时作为成员存在于多个房间。
   - **房主身份同样可以**作为成员加入多个他人房间，其房主身份不受影响；限制仅针对「拥有」。
5. **房间自动清理（定时任务，兜底）**：
   - 定时扫描「**无 owner** 且 `member_count == 0` 且持续超过 N 分钟（默认 5 分钟）」的房间。
   - 自动删除房间并级联清理（成员关联、邀请码、通知、消息）。
   - 通过 `notice_socket` 推送 `room_closed` 事件通知相关用户。
   - **多实例安全**：任务加分布式锁（Redis），避免多实例重复执行（见第 10 节）。

### 7.3 数据模型

```
Room:
  room_id        String
  room_name      String
  owner_id       String    房主 user_id（唯一：每账号最多 1 个房主房间）
  created_at     Long
  status         active / closed
  member_count   Int      当前关联用户数
  online_count   Int      当前在线用户数（Redis 聚合）
  empty_since    Long?    成员清空时间（用于自动删除判断）
```

---

## 8. Socket 协议定义

### 8.0 连接鉴权（两个 socket 通用）

- 连接时校验 JWT（`Authorization` / query `token`），并校验 `token` 中的
  `user_id` 与连接参数一致（`room_socket` 还校验 `room_id`）。
- 鉴权失败直接拒绝连接；重连 token 需重新校验。
- 服务端所有权限类操作（踢人、改角色、房主拒绝等）按 `user_id` 的角色二次校验，不信任客户端上报身份。

### 8.1 room_socket（8085）

连接：`ws://{host}:8085/ws?room={room_id}&user={user_id}&token=<jwt>`

| 消息类型 | 方向 | 说明 |
|---|---|---|
| `ping` / `pong` | 双向 | 心跳，3 次无响应判离线（第 2 节） |
| `chat_message` | C→S / S→C | 聊天消息（携带 client_msg_id / seq） |
| `history_sync` | C→S / S→C | 重连按 seq 增量补拉断线消息（第 5 节） |
| `member_joined` | S→C | 新成员加入 |
| `member_online_status_changed` | S→C | 成员在线/离线状态变更 |
| `member_kicked` | S→C | 被踢出通知 |
| `role_updated` | S→C | 角色变更通知 |
| `mute` / `unmute` / `mic_disable` / `mic_enable` | S→C | 禁言/禁麦控制 |
| `speaking_start` / `speaking_stop` | S→C | 说话状态广播 |
| `room_closed` | S→C | 房间被关闭 |

> 已移除 `client_leave`：离线由心跳判定，客户端断开连接即可，无需显式离开消息。

### 8.2 notice_socket（custom 端口，跨房间）

连接：`ws://{host}:{custom}/ws/notice?user={user_id}&token=<jwt>`

| 消息类型 | 方向 | 说明 |
|---|---|---|
| `invite_request` | C→S | 发送邀请（限流，见 3.4） |
| `invite_accept` / `invite_reject` | C→S | 接受/拒绝邀请 |
| `invite_received` / `invite_accepted` / `invite_rejected` | S→C | 邀请通知推送 |
| `knock_request` | C→S | 敲门（含 room_id、留言；限流见 3.4） |
| `knock_accept` / `knock_reject` | C→S | 房主接受/拒绝敲门 |
| `knock_received` / `knock_accepted` / `knock_rejected` | S→C | 敲门通知推送 |
| `notification` | S→C | 通用通知推送（失败重试，见 6.2） |
| `notification_sync` | S→C | 重连补推未读通知（见 6.2） |
| `room_closed` | S→C | 房间关闭通知（跨房间） |

### 8.3 通用事件结构

```
WebSocketEvent:
  event_id       String
  type           String    事件类型
  room_id        String?
  user_id        String?
  operator_id    String?
  target_user_id String?
  data           JSON?
  timestamp      Long
```

---

## 9. 数据库设计（表清单）

| 表 | 关键字段 | 说明 |
|---|---|---|
| `users` | user_id, username, room_id, last_login_at | 聊天用户 + 当前房间关联 |
| `rooms` | room_id, room_name, owner_id, status, member_count, online_count, empty_since | 房间 |
| `room_members` | user_id, room_id, role, status, online_status, joined_at, offline_at, last_active | 成员关系（一人可多房间） |
| `invite_codes` | code, room_id, created_by, target_user_id, status, used_by, created_at, expires_at, used_at | 邀请码 |
| `invite_records` | id, from_user_id, to_user_id, room_id, status, invite_code, created_at | 邀请记录 |
| `knock_records` | id, user_id, room_id, message, status, invite_code, created_at | 敲门记录 |
| `messages` | id, room_id, user_id, client_msg_id, seq, type, content, file_name, file_size, mime_type, width, height, timestamp | 聊天消息 |
| `notifications` | id, user_id, type, title, content, room_id, related_user_id, data, is_read, created_at, read_at | 通知 |
| `user_bindings` | bus_user_id, chat_user_id | bus → chat 映射（resolve 用） |

### 9.1 索引建议

- `rooms(owner_id)` **唯一索引**（每账号最多 1 个房主房间）、`room_members(room_id, status)`
- `messages(room_id, seq)`（顺序/分页）、`messages(user_id, client_msg_id)` **唯一索引**（消息幂等）
- `invite_codes(code)` 唯一索引、`invite_codes(room_id, status)`
- `notifications(user_id, is_read, created_at)`

---

## 10. 定时任务（服务端内部）

| 任务 | 周期 | 逻辑 |
|---|---|---|
| 心跳超时检测 | 每次 ping 周期 | 3 次无响应 → 标记离线，记录 `offline_at`，推送 `member_online_status_changed`（不解除房间关联） |
| 邀请码过期 | 如每 60s | 将超期未用码 `unused → expired` |
| 空房间清理 | 如每 60s | `member_count == 0` 且超过 5 分钟 → 删除房间并级联清理、推送 `room_closed` |

- 定时任务均需**分布式锁**（Redis `SETNX` + TTL + 续租），保证多实例部署时不重复执行。
- 心跳检测建议由连接层驱动（在 Redis 内存中判定），不依赖 DB 轮询。

---

## 11. 错误码约定

| code | 含义 | 场景 |
|---|---|---|
| 0 | 成功 | - |
| 403 | 无权限/邀请码无效 | `invalid or missing invite code` |
| 409 | 冲突 | 并发覆盖房间等一般资源冲突（已拥有房间时创建房间走「覆盖」语义，不报 409） |
| 404 | 资源不存在 | 房间/用户/通知不存在 |
| 429 | 触发限流 | 邀请/敲门/邀请码校验超频（见 3.4） |

统一响应结构：`{ "code": 0, "message": "", "data": ... }`，`code == 0` 为成功。

---

## 12. 非功能性需求

1. **并发与连接管理**：`room_socket` / `notice_socket` 需支持高并发长连接，心跳与重连
   需保证连接资源及时回收；连接鉴权失败立即断开并清理资源。
2. **存储分层**：在线状态/在线人数/有效邀请码等热数据存 Redis（TTL 兜底），
   落库采用最终一致，避免长连接状态写放大 DB。
3. **幂等性**：邀请码一次性使用用 CAS 原子更新（影响行数 == 1 才算成功）；消息发送用
   `(user_id, client_msg_id)` 去重；每账号最多 1 个房主房间用 `rooms(owner_id)` 唯一索引 + 事务兜底。
4. **消息顺序**：消息按房间内自增 `seq` 排序，广播与历史查询一致；断线重连按 `seq` 增量补拉。
5. **推送可靠性**：`notice_socket` 推送失败走失败队列 + 指数退避重试；目标离线则落库待重连补投。
6. **安全**：
   - Socket 连接鉴权绑定 user_id / room_id，权限操作按角色二次校验。
   - 邀请/敲门/邀请码校验限流（429），邀请码校验失败锁定防爆破。
   - 文件上传白名单 + 服务端 MIME 二次校验 + 病毒扫描 + 私有存储 + 签名 URL。
7. **可观测性**：metrics（连接数、在线数、消息吞吐、通知推送延迟/失败率）+ 审计日志
   （离线时间、邀请码生成/使用、房间创建/删除、踢人等关键操作）。
8. **可扩展性**：`notice_socket` 消息类型需设计为可扩展（通用事件结构，见 8.3）；
   定时任务支持多实例分布式执行（分布式锁）。
