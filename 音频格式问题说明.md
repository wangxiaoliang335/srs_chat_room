# 音频格式不匹配问题说明

## 问题概述

客户端分析和日志检测发现以下两个问题：

| 问题 | 预期值 | 实际值 |
|------|--------|--------|
| 采样率 | 16kHz | 44100Hz |
| 声道数 | 单声道 | 立体声 |

同时，`decodedFrames: 0` 说明 flv.js 未成功解码音频。

---

## 问题根源分析

### 问题一：FFmpeg 转码参数未正确生效

当前 `electron/main.ts` 的 FFmpeg 参数：

```javascript
const ffmpegArgs = [
  '-f', 's16le',
  '-ar', sampleRate.toString(),  // 采样率
  '-ac', channels.toString(),    // 声道数
  '-i', 'pipe:0',
  '-c:a', 'aac',
  '-b:a', bitrate,
  '-f', 'flv',
  rtmpUrl
];
```

**问题原因**：`-ar` 和 `-ac` 参数只是告诉 FFmpeg **输入数据** 的格式，而不是强制 **输出格式**。如果输入的 PCM 本身就是 44.1kHz 立体声，FFmpeg 会直接传递，不会重新采样。

### 问题二：需要使用音频过滤器强制重采样

**解决方案**：必须添加 `-af` (audio filter) 来强制重采样。

---

## 修复方案

### 方案一：正确添加音频过滤器

**当前写法（问题代码）**：

```bash
ffmpeg -f s16le -ar 44100 -ac 2 -i pipe:0 -c:a aac -f flv rtmp://...
```

**正确写法**：

```bash
ffmpeg -f s16le -ar 44100 -ac 2 -i pipe:0 -af "aresample=16000,pan=mono|c0=c0" -c:a aac -f flv rtmp://...
```

### 方案二：修改 electron/main.ts 代码

将第 219-231 行修改为：

```javascript
const ffmpegArgs = [
  '-f', 's16le',
  '-ar', inputSampleRate.toString(),  // 输入采样率（原数据格式）
  '-ac', inputChannels.toString(),    // 输入声道数（原数据格式）
  '-i', 'pipe:0',
  '-af', `aresample=${outputSampleRate}:filter_size=32:force_resampling=1`,
  '-ac', outputChannels.toString(),   // 输出声道数
  '-c:a', 'aac',
  '-b:a', bitrate,
  '-f', 'flv',
  '-reconnect', '1',
  '-reconnect_at_eof', '1',
  '-reconnect_streamed', '1',
  rtmpUrl
];
```

### 方案三：更新 IPC 接口参数

将接口参数从单一参数拆分为输入和输出：

```typescript
interface FFmpegStreamConfig {
  rtmpUrl: string;
  inputSampleRate?: number;   // 输入音频的实际采样率
  inputChannels?: number;     // 输入音频的实际声道数
  outputSampleRate?: number;  // 期望输出的采样率（默认 16000）
  outputChannels?: number;    // 期望输出的声道数（默认 1）
  bitrate?: string;
}
```

---

## 需要确认的信息

修改代码前请确认：

1. **当前发送的音频源格式是什么？**（麦克风采集的格式）
2. **期望输出的格式是什么？**（16kHz 单声道？）
3. **修改哪一端？**（Electron 端 / 服务端 / 两端都要改）

---

## 客户端需修改的文件

- `electron/main.ts` - FFmpeg 转码参数配置

---

## 问题修复后验证

1. 音频输出应为 **16kHz 单声道**
2. SRS 服务器检测到的格式应与预期一致
3. `decodedFrames` 应大于 0，音频可正常播放
