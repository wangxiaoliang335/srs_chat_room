# 房间生命周期管理 — 服务端需求文档

## 1. 概述

规范房间从创建到关闭的完整生命周期。核心变更：

1. **不再对外暴露房间列表接口**（`GET /api/v1/rooms`），房间入口仅通过邀请或直接指定 room_id 进入。
2. **房主离开房间不转让房主**，保留房主占位（owner placeholder），房间仍可运行。
3. **房主进入其他房间时自动关闭原房间**，本地监听 + 通知后端执行关闭。
4. **新增「获取用户房间」接口**，用于查找好友的房间，闭环"申请加入好友房间"流程。

### 涉及文档

- `E:\Project\Chat\docs\服务端文档.md`（现有 API 规范，本需求保持风格一致）
- `E:\Project\Chat\docs\CHAT_MODULE.md`

---

## 2. 概念模型

### 2.1 房间状态

| 状态 | 说明 |
|------|------|
| `active` | 房间正常运行，成员可进出和发言 |
| `owner_offline` | 房主已离开房间/离线，房间仍在运行，其他成员不受影响 |
| `closed` | 房间已被关闭，不可再加入 |

### 2.2 房主占位机制

- 房主离开房间后，**不转让房主**，房主在房间成员列表中仍然存在，但标记为 `owner_offline`。
- 房间其他人角色不变（`member`），且不能获得房主权限（禁言、踢人等操作禁用）。
- 房主重新进入房间时，恢复为 `owner_online` 状态。
- 房间成员可看到房主状态：`online` / `offline`。

### 2.3 房间关闭条件

房主**只有主动进入另一个房间时**，才触发原房间的关闭流程：
1. 客户端检测到当前用户是房主且已有 active 房间
2. 用户尝试进入另一个房间
3. 客户端先调用**关闭原房间**接口（`DELETE /api/v1/rooms/{room_id}`）
4. 关闭成功后再加入新房间
5. 服务端向原房间广播 `room_closed` 事件

> 注意：是**关闭房间**，不是转让。

---

## 3. HTTP API

基础 URL：`http://{host}:8085`

> 以下接口均需 `Authorization: Bearer <聊天服务器 JWT>`。

### 3.1 获取指定用户的房间（查找好友房间）

```
GET /api/v1/users/{user_id}/room
```

**说明：** 查询指定用户（通常是好友）当前所在的 active 房间。用于"申请加入好友房间"流程的第一步。

**逻辑：**
1. 查询 `user_id` 作为房主的 active 房间
2. 如果用户不在任何 active 房间中（既不是房主也不是成员），返回 `data: null`
3. 如果用户在房间中但不是房主，返回其所在房间信息（角色标记为 `member`）

**成功响应（有房间）：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "room_id": "room_xxx",
    "room_name": "闲聊室",
    "owner_id": "user_xxx",
    "owner_status": "online",
    "your_role": "member",
    "member_count": 3,
    "online_count": 1
  }
}
```

**无房间时响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

**错误码：**

| HTTP 状态码 | code | message | 说明 |
|-------------|------|---------|------|
| 404 | 404 | user not found | 用户不存在 |

### 3.2 检测房间是否存活

```
GET /api/v1/rooms/{room_id}/health
```

**说明：** 检测房间是否存在且可用（未被关闭）。轻量接口，用于客户端进入房间前做前置检查。

**校验规则：**

| HTTP 状态码 | code | message | 说明 |
|-------------|------|---------|------|
| 404 | 404 | room not found | 房间不存在或已被关闭 |
| 200 | 0 | success | 房间存活 |

**存活响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "room_id": "room_xxx",
    "status": "active",
    "owner_id": "user_xxx",
    "owner_status": "online",
    "member_count": 3
  }
}
```

**不存活响应：**

```json
{
  "code": 404,
  "message": "room not found or closed",
  "data": null
}
```

### 3.3 关闭房间

```
DELETE /api/v1/rooms/{room_id}
```

> 此接口已存在，行为不变，但需增加以下业务逻辑：

**附加逻辑（新增）：**
1. 仅房主可以关闭房间（已有）
2. 关闭房间后，在房间成员列表中保留房主的**关闭记录**（owner 标记 + 房间已关闭）
3. 关闭房间时，向房间内所有在线成员广播 WS 事件 `room_closed`

**成功响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

### 3.4 离开房间（房主版本）

```
POST /api/v1/rooms/{room_id}/leave
```

**说明：** 现有接口，需为房主增加以下逻辑：

**房主离开时的逻辑（新增）：**
1. 房主离开房间后，房间状态不变化（仍为 `active`）
2. 房主在成员列表中的状态标记为 `owner_offline`
3. **不触发转让**，没有新房主
4. 向房间内其他成员广播 WS 事件 `owner_offline`
5. 房主重新加入时，状态恢复为 `owner_online`，广播 `owner_online`

**请求体：**

```json
{
  "user_id": "user_xxx"
}
```

**成功响应：**

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

## 4. WebSocket 事件

房间通知 WebSocket（端口 8085 `/ws`）新增以下事件类型：

### 4.1 `owner_offline` — 房主离线/离开

推送给房间内所有**非房主成员**。

```json
{
  "type": "owner_offline",
  "room_id": "room_xxx",
  "data": {
    "owner_id": "user_xxx",
    "owner_name": "Alice",
    "timestamp": 1785306254
  },
  "timestamp": "2026-07-28T10:00:00Z"
}
```

### 4.2 `owner_online` — 房主上线/重新进入

推送给房间内所有**非房主成员**。

```json
{
  "type": "owner_online",
  "room_id": "room_xxx",
  "data": {
    "owner_id": "user_xxx",
    "owner_name": "Alice",
    "timestamp": 1785306254
  },
  "timestamp": "2026-07-28T10:00:00Z"
}
```

### 4.3 `room_closed` — 房间已关闭

由房主关闭房间时触发，推送给房间内所有在线成员。

```json
{
  "type": "room_closed",
  "room_id": "room_xxx",
  "data": {
    "closed_by": "user_xxx",
    "reason": "owner_entered_another_room",
    "timestamp": 1785306254
  },
  "timestamp": "2026-07-28T10:00:00Z"
}
```

---

## 5. 客户端流程

### 5.1 申请加入好友房间

```
1. 用户 A 在好友列表中选择用户 B（好友）
2. 客户端调用 GET /api/v1/users/{user_b_id}/room
   → 如果 data == null → 提示"该好友暂未开启房间"
   → 如果 data != null → 得到好友房间 ID

3. 客户端调用 GET /api/v1/rooms/{room_id}/health
   → 如果房间不存在 → 提示"房间已关闭"
   → 如果房间存活 → 继续

4. 客户端调用 POST /api/v1/rooms/{room_id}/knock（敲门/请求加入）
   → 向房主发送加入请求
```

### 5.2 房主进入房间（含关闭旧房间）

```
1. 客户端调用 GET /api/v1/users/{user_id}/room（可省略，客户端本地已有记录）
   或直接从本地缓存获取当前用户的房间 ID

2. 客户端调用 GET /api/v1/rooms/{target_room_id}/health
   → 如果房间不存在 → 提示"房间已关闭"
   → 如果房间存活 → 继续

3. 如果当前用户是房主且已有 active 房间（且与目标房间不同）：
   a. 调用 DELETE /api/v1/rooms/{current_room_id} 关闭原房间
   b. 等待关闭成功
   c. 调用 POST /api/v1/rooms/{target_room_id}/join 加入新房间
   否则直接加入新房间
```

### 5.3 离开房间流程（房主）

```
1. 调用 POST /api/v1/rooms/{room_id}/leave
2. 服务端将房主标记为 owner_offline
3. 服务端广播 owner_offline 给其他成员
4. 房主客户端回到房间列表/主界面
```

### 5.4 重新进入房间流程（房主）

```
1. 调用 POST /api/v1/rooms/{room_id}/join
2. 服务端检测到房主重新加入，将状态恢复为 owner_online
3. 服务端广播 owner_online 给其他成员
```

---

## 6. 数据模型变更

### 6.1 房间数据扩展

现有房间数据模型增加字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `owner_status` | string | `online` / `offline`，房主当前在线状态 |
| `closed_by` | string | 关闭者 user_id（仅在 closed 时有值） |
| `closed_at` | int | 关闭时间戳（仅在 closed 时有值） |
| `close_reason` | string | 关闭原因（如 `owner_entered_another_room`） |

### 6.2 成员模型扩展

| 字段 | 类型 | 说明 |
|------|------|------|
| `is_owner` | bool | 是否为房主 |
| `owner_status` | string | 仅当 `is_owner=true` 时有意义：`online` / `offline` |

---

## 7. 与现有功能的关系

| 功能 | 关系 |
|------|------|
| `GET /api/v1/rooms` 房间列表 | **不再对外暴露**，移除或仅用于管理后台 |
| `POST /api/v1/rooms/{id}/join` 加入房间 | 逻辑不变，房主重新加入时额外恢复 owner_status |
| `POST /api/v1/rooms/{id}/leave` 离开房间 | 房主离开时新增 owner_offline 逻辑 |
| `DELETE /api/v1/rooms/{id}` 关闭房间 | 房主关闭房间时新增 room_closed 广播 |
| 邀请功能 | 不受影响，房主离线时受邀者仍可被邀请 |
| 敲门功能 | 不受影响，用于"申请加入好友房间"流程的第 4 步 |

---

## 8. 错误码汇总

| HTTP 状态码 | code | message | 说明 |
|-------------|------|---------|------|
| 200 | 0 | success | 成功 |
| 400 | 400 | room not active | 房间不是 active 状态 |
| 400 | 400 | owner already has active room | 房主已有 active 房间（创建新房间时） |
| 400 | 400 | not room owner | 非房主尝试关闭房间 |
| 404 | 404 | room not found | 房间不存在或已关闭 |
| 401 | 401 | not authenticated | JWT 缺失或无效 |
