# 说话状态对接文档

本文档单独说明“客户端开始说话 / 停止说话”相关的真实对接链路，包括：

1. 客户端开始说话时，服务端实际接收的接口
2. 客户端停止说话时，服务端实际接收的接口
3. 服务端如何把说话状态通知给房间内其他人
4. 客户端如何查询当前谁正在说话

> 重要说明：
>
> 当前系统里的“开始说话 / 停止说话”语义，实际上是 **开始推流 / 停止推流**。
> 也就是说，它不是逐帧语音活动检测（VAD），而是当服务端收到音频流发布/停止回调后更新状态并广播事件。

---

## 一、整体链路

### 1.1 开始说话链路

```text
客户端开始推音频流
    -> SRS 触发 on_publish 回调
    -> 服务端解析 stream_name 得到 room_id / user_id
    -> 服务端将该用户标记为 speaking
    -> 服务端向房间内其他订阅者广播 user_speaking_start
```

### 1.2 停止说话链路

```text
客户端停止推音频流
    -> SRS 触发 on_unpublish 回调
    -> 服务端解析 stream_name 得到 room_id / user_id
    -> 服务端将该用户从 speaking 列表中移除
    -> 服务端向房间内其他订阅者广播 user_speaking_stop
```

---

## 二、客户端“开始说话”通知服务器的接口

严格来说，**不是客户端直接调用一个“开始说话”HTTP 接口**，而是客户端开始推流后，由 **SRS 回调服务端**。

### 2.1 SRS 发布流回调

- **路径**: `/api/v1/streams/on_publish`
- **方法**: `POST`
- **调用方**: SRS
- **业务语义**: 某个音频流开始发布

### 请求示例

```json
{
  "stream": "room_123_user_001",
  "tcUrl": "rtmp://<server>/live",
  "client_ip": "192.168.1.10"
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `stream` | string | 是 | 流名称。服务端通过它解析 `room_id` 和 `user_id` |
| `tcUrl` | string | 否 | 推流地址 |
| `client_ip` | string | 否 | 推流客户端 IP |

### 服务端处理逻辑

当这是一个“原始音频流”时，服务端会：

1. 从 `stream` 解析出 `room_id`、`user_id`
2. 检查房间是否存在
3. 检查用户是否已经加入该房间
4. 将该用户加入当前说话列表
5. 广播 `user_speaking_start`

### 响应示例

```json
{
  "code": 0
}
```

### 对接注意事项

1. 客户端必须先加入房间，否则即使推流，服务端也不会把它作为有效说话用户处理。
2. 文档和代码当前都要求用户先调用加入房间接口：

- `POST /api/v1/room/<room_id>/join`

3. 如果客户端被禁言或禁麦，应先查询成员状态中的 `publish_allowed`。

---

## 三、客户端“停止说话”通知服务器的接口

同样地，**不是客户端直接调“停止说话”接口**，而是客户端停止推流后，由 **SRS 回调服务端**。

### 3.1 SRS 停止发布回调

- **路径**: `/api/v1/streams/on_unpublish`
- **方法**: `POST`
- **调用方**: SRS
- **业务语义**: 某个音频流停止发布

### 请求示例

```json
{
  "stream": "room_123_user_001"
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `stream` | string | 是 | 流名称。服务端通过它解析 `room_id` 和 `user_id` |

### 服务端处理逻辑

当这是一个“原始音频流”停止发布时，服务端会：

1. 从 `stream` 解析出 `room_id`、`user_id`
2. 从当前说话列表中移除该用户
3. 广播 `user_speaking_stop`

### 响应示例

```json
{
  "code": 0
}
```

---

## 四、推流侧 `stream` 命名规范（非常重要）

服务端是否能正确识别“谁开始说话 / 谁停止说话”，取决于 SRS 回调里的 `stream` 字段是否符合当前解析规则。

如果 `stream` 命名不符合规则，服务端就无法从中正确解析出：

- `room_id`
- `user_id`
- 是否为原始音频流还是翻译流

这会直接导致：

- `on_publish` / `on_unpublish` 无法正确识别说话用户
- `user_speaking_start` / `user_speaking_stop` 无法正确广播
- 翻译流与原始流无法正确区分

### 4.1 原始音频流命名格式

当前代码支持两种原始音频流命名格式：

#### 格式 A：`room数字_user_id`

```text
{room_id}_{user_id}
```

示例：

```text
room1777389806400_user_70548
```

解析结果：

- `room_id = room1777389806400`
- `user_id = user_70548`

#### 格式 B：`room_数字_user_id`

```text
{room_id}_{user_id}
```

示例：

```text
room_1778408735818_1
```

解析结果：

- `room_id = room_1778408735818`
- `user_id = 1`

### 4.2 翻译流命名格式

翻译流通过是否包含 `_to_` 来识别。

格式：

```text
{room_id}_{source_user}_to_{lang}
```

示例：

```text
room1777389806400_user_70548_to_en
room_1778408735818_1_to_zh
```

解析结果：

- `type = translation`
- `room_id = room1777389806400` 或 `room_1778408735818`
- `user_id = source_user`
- `to_lang = en` 或 `zh`

### 4.3 服务端当前识别规则

当前逻辑可概括为：

1. 如果 `stream` 中包含 `_to_`，按**翻译流**解析
2. 否则按**原始音频流**解析
3. `room_id` 必须以 `room` 开头，否则无法识别

### 4.4 推荐命名规范

虽然当前代码兼容两种 `room_id` 格式，但为了减少歧义，建议统一使用：

#### 原始音频流

```text
room_<roomNumericId>_<userId>
```

示例：

```text
room_1778408735818_user_001
```

#### 翻译流

```text
room_<roomNumericId>_<sourceUserId>_to_<lang>
```

示例：

```text
room_1778408735818_user_001_to_en
```

### 4.5 不推荐或会解析失败的命名方式

以下命名方式不要使用：

#### 错误示例 1：不以 `room` 开头

```text
chat_123_user_001
```

问题：
- 服务端找不到 `room` 前缀
- 无法解析 `room_id`

#### 错误示例 2：缺少用户 ID

```text
room_1778408735818
```

问题：
- 无法解析出 `user_id`

#### 错误示例 3：随意改变翻译流分隔符

```text
room_1778408735818_user_001_toen
room_1778408735818_user_001-en
```

问题：
- 服务端识别翻译流依赖 `_to_`
- 不符合规则就会识别失败

#### 错误示例 4：房间 ID 与用户 ID 拼接规则混乱

```text
room1777389806400user_001
```

问题：
- 缺少合法分隔符
- 无法解析 `room_id` 和 `user_id`

### 4.6 客户端 / 推流侧落地建议

如果你们的 RTC / RTMP / WebRTC 推流端可以自定义 stream 名称，建议直接在推流前统一构造：

```text
原始音频流: {room_id}_{user_id}
翻译流: {room_id}_{source_user}_to_{lang}
```

并保证：

1. `room_id` 始终以 `room` 开头
2. 原始流和翻译流严格使用上面的分隔格式
3. 不要临时插入额外前缀、后缀或随机字符串
4. 客户端加入房间使用的 `room_id`，要和 `stream` 中的 `room_id` 保持一致

---

## 五、服务端通知房间内其他人的接口

服务端对房间内其他客户端的通知，不是通过普通 HTTP 响应返回，而是通过实时通道广播。

当前支持两种下行方式：

1. **Socket.IO**（推荐）
2. **原生 WebSocket**（兼容）

---

## 六、Socket.IO 下行通知

### 7.1 连接地址

```text
http://<server-host>:8085
```

### 6.2 订阅房间

客户端连接后发送事件：`subscribe`

```json
{
  "room_id": "room_123",
  "user_id": "user_002"
}
```

订阅成功返回：

```json
{
  "type": "subscribed",
  "room_id": "room_123",
  "user_id": "user_002"
}
```

### 6.3 监听房间广播事件

客户端应监听事件名：

```text
room_event
```

### 6.4 开始说话事件 `user_speaking_start`

```json
{
  "type": "user_speaking_start",
  "data": {
    "event_id": "a1b2c3d4",
    "room_id": "room_123",
    "user_id": "user_001",
    "operator_id": "",
    "target_user_id": "",
    "data": {
      "stream_url": "http://<srs-host>/live/room_123_user_001"
    },
    "timestamp": "2026-06-07 10:10:10",
    "type": "user_speaking_start"
  }
}
```

### 6.5 停止说话事件 `user_speaking_stop`

```json
{
  "type": "user_speaking_stop",
  "data": {
    "event_id": "e5f6g7h8",
    "room_id": "room_123",
    "user_id": "user_001",
    "operator_id": "",
    "target_user_id": "",
    "data": {},
    "timestamp": "2026-06-07 10:12:30",
    "type": "user_speaking_stop"
  }
}
```

### 6.6 字段说明

| 字段路径 | 类型 | 说明 |
|----------|------|------|
| `type` | string | 外层事件类型 |
| `data.event_id` | string | 事件唯一 ID |
| `data.room_id` | string | 房间 ID |
| `data.user_id` | string | 开始/停止说话的用户 ID |
| `data.data.stream_url` | string | 推流地址，仅开始说话事件通常存在 |
| `data.timestamp` | string | 服务端事件时间 |
| `data.type` | string | 内层事件类型，与外层 `type` 一致 |

---

## 七、原生 WebSocket 下行通知

### 7.1 连接地址

```text
ws://<server-host>:8086/ws?room=<room_id>&user=<user_id>
```

示例：

```text
ws://example.com:8086/ws?room=room_123&user=user_002
```

### 7.2 连接成功消息

```json
{
  "type": "connected",
  "room_id": "room_123",
  "user_id": "user_002",
  "room_info": {
    "online_users": 2,
    "translation_status": "idle"
  }
}
```

### 7.3 开始说话事件 `user_speaking_start`

```json
{
  "event_id": "a1b2c3d4",
  "room_id": "room_123",
  "user_id": "user_001",
  "operator_id": "",
  "target_user_id": "",
  "data": {
    "stream_url": "http://<srs-host>/live/room_123_user_001"
  },
  "timestamp": "2026-06-07 10:10:10",
  "type": "user_speaking_start"
}
```

### 7.4 停止说话事件 `user_speaking_stop`

```json
{
  "event_id": "e5f6g7h8",
  "room_id": "room_123",
  "user_id": "user_001",
  "operator_id": "",
  "target_user_id": "",
  "data": {},
  "timestamp": "2026-06-07 10:12:30",
  "type": "user_speaking_stop"
}
```

---

## 八、客户端查询当前谁在说话

客户端在进入房间后，如果要先拿一份当前状态，可以调这个 HTTP 接口。

### 8.1 获取当前正在说话的用户列表

- **路径**: `/api/v1/room/<room_id>/speaking`
- **方法**: `GET`

### 请求示例

```text
GET /api/v1/room/room_123/speaking
```

### 响应示例

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "room_id": "room_123",
    "speaking_users": [
      "user_001",
      "user_003"
    ]
  }
}
```

### 适用场景

- 客户端刚进房，先同步当前状态
- 防止错过进入房间前已经发生的 `user_speaking_start`

建议流程：

1. 先加入房间
2. 建立 Socket.IO / 原生 WebSocket 连接并订阅
3. 调用 `/api/v1/room/<room_id>/speaking` 获取当前说话列表
4. 后续再依赖实时事件增量更新 UI

---

## 九、前端最小接入示例

### 9.1 Web 端 Socket.IO 示例（推荐）

下面示例展示一个最小可用流程：

1. 先加入房间
2. 建立 Socket.IO 连接
3. 发送 `subscribe`
4. 监听 `room_event`
5. 用 `/speaking` 初始化当前说话状态

```javascript
import { io } from "socket.io-client";

const serverHost = "http://localhost:8085";
const roomId = "room_123";
const userId = "user_002";

const speakingUsers = new Set();

async function joinRoom() {
  const resp = await fetch(`${serverHost}/api/v1/room/${roomId}/join`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ user_id: userId, role: "member" })
  });

  const data = await resp.json();
  if (data.code !== 0) {
    throw new Error(`join room failed: ${data.message}`);
  }
}

async function loadSpeakingUsers() {
  const resp = await fetch(`${serverHost}/api/v1/room/${roomId}/speaking`);
  const data = await resp.json();

  if (data.code !== 0) {
    throw new Error(`load speaking users failed: ${data.message}`);
  }

  speakingUsers.clear();
  for (const uid of data.data.speaking_users || []) {
    speakingUsers.add(uid);
  }

  renderSpeakingUsers();
}

function renderSpeakingUsers() {
  console.log("当前说话中的用户:", Array.from(speakingUsers));
}

async function startRealtime() {
  await joinRoom();

  const socket = io(serverHost, {
    transports: ["websocket", "polling"]
  });

  socket.on("connect", () => {
    console.log("socket connected");
    socket.emit("subscribe", {
      room_id: roomId,
      user_id: userId
    });
  });

  socket.on("subscribed", (payload) => {
    console.log("subscribed:", payload);
  });

  socket.on("room_event", (event) => {
    console.log("room_event:", event);

    const eventType = event?.type;
    const eventData = event?.data || {};
    const speakingUserId = eventData.user_id;

    if (eventType === "user_speaking_start" && speakingUserId) {
      speakingUsers.add(speakingUserId);
      renderSpeakingUsers();
      return;
    }

    if (eventType === "user_speaking_stop" && speakingUserId) {
      speakingUsers.delete(speakingUserId);
      renderSpeakingUsers();
    }
  });

  socket.on("error", (payload) => {
    console.error("socket error:", payload);
  });

  socket.on("disconnect", () => {
    console.log("socket disconnected");
  });

  await loadSpeakingUsers();

  setInterval(() => {
    socket.emit("ping", {});
  }, 30000);
}

startRealtime().catch(console.error);
```

### 9.2 原生 WebSocket 示例（兼容）

```javascript
const wsHost = "ws://localhost:8086";
const roomId = "room_123";
const userId = "user_002";
const speakingUsers = new Set();

function renderSpeakingUsers() {
  console.log("当前说话中的用户:", Array.from(speakingUsers));
}

async function loadSpeakingUsers() {
  const resp = await fetch(`http://localhost:8085/api/v1/room/${roomId}/speaking`);
  const data = await resp.json();

  speakingUsers.clear();
  for (const uid of data.data.speaking_users || []) {
    speakingUsers.add(uid);
  }

  renderSpeakingUsers();
}

function startNativeWebSocket() {
  const ws = new WebSocket(`${wsHost}/ws?room=${roomId}&user=${userId}`);

  ws.onopen = () => {
    console.log("native ws connected");
  };

  ws.onmessage = (message) => {
    const event = JSON.parse(message.data);
    console.log("native ws event:", event);

    if (event.type === "connected") {
      loadSpeakingUsers().catch(console.error);
      return;
    }

    if (event.type === "user_speaking_start" && event.user_id) {
      speakingUsers.add(event.user_id);
      renderSpeakingUsers();
      return;
    }

    if (event.type === "user_speaking_stop" && event.user_id) {
      speakingUsers.delete(event.user_id);
      renderSpeakingUsers();
    }
  };

  ws.onerror = (err) => {
    console.error("native ws error:", err);
  };

  ws.onclose = () => {
    console.log("native ws closed");
  };

  setInterval(() => {
    if (ws.readyState === WebSocket.OPEN) {
      ws.send(JSON.stringify({ type: "ping" }));
    }
  }, 30000);
}

startNativeWebSocket();
```

---

## 十、客户端接入建议

### 10.1 推荐方案

推荐客户端按下面方式接：

1. 调用加入房间接口
2. 建立 Socket.IO 连接
3. 发送 `subscribe`
4. 监听 `room_event`
5. 收到：
   - `user_speaking_start` -> 把该用户标记为正在说话
   - `user_speaking_stop` -> 取消该用户的说话状态
6. 初次进入房间时调用 `/api/v1/room/<room_id>/speaking` 做一次状态补齐

### 10.2 需要特别注意

1. 当前没有“客户端直接调用开始说话/停止说话 HTTP 接口”的设计。
2. 真正触发说话状态变化的是：
   - SRS `on_publish`
   - SRS `on_unpublish`
3. 因此客户端如果想触发“开始说话”，本质上是**开始推流**；想触发“停止说话”，本质上是**停止推流**。
4. 如果未来需要更细粒度的“开口/闭嘴”状态，需要单独增加 VAD 检测链路，不能直接复用当前语义。

---

## 十一、接口汇总

### 11.1 上行到服务端

| 用途 | 路径 / 事件 | 方法 / 类型 | 调用方 |
|------|-------------|-------------|--------|
| 加入房间 | `/api/v1/room/<room_id>/join` | POST | 客户端 |
| 开始说话（实际是开始推流后的回调） | `/api/v1/streams/on_publish` | POST | SRS |
| 停止说话（实际是停止推流后的回调） | `/api/v1/streams/on_unpublish` | POST | SRS |

### 11.2 下行到房间内其他人

| 通道 | 订阅方式 | 事件 |
|------|----------|------|
| Socket.IO | `subscribe` | `room_event -> user_speaking_start / user_speaking_stop` |
| 原生 WebSocket | 连接或 `subscribe` | `user_speaking_start / user_speaking_stop` |

### 11.3 状态查询

| 用途 | 路径 | 方法 |
|------|------|------|
| 获取当前说话用户列表 | `/api/v1/room/<room_id>/speaking` | GET |
