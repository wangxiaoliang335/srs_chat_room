# 语音聊天室 - 客户端接口综合文档

> **适用版本**：FastAPI 回调服务 v1 / SRS 流媒体 v1
> **文档版本**：v1.0（2026-06-19）

---

## 一、基础信息

| 项目 | 说明 |
|------|------|
| 默认端口 | 8085（HTTP API + WebSocket） |
| 协议 | HTTP/HTTPS |
| 响应格式 | JSON |
| 编码 | UTF-8 |
| 测试地址 | `http://47.107.33.154:8085` |

### 1.1 流媒体地址

| 用途 | 地址 | 协议 | 说明 |
|------|------|------|------|
| 推流（主播端） | `rtmp://<server>:1935/live/{stream_key}` | RTMP | 用 ffmpeg / OBS 推到 SRS |
| 拉流（观众端） | `http://<server>:8080/live/{stream_key}.flv` | HTTP-FLV | 浏览器原生 / flv.js |
| 拉流（低延迟） | `webrtc://<server>:1985/live/{stream_key}` | WebRTC | SRS v4+ |
| 业务 HTTP | `http://<server>:8085/api/v1/...` | HTTP/JSON | 所有业务接口 |
| WebSocket | `ws://<server>:8085/ws` | WS | 房间事件实时通知 |

### 1.2 流命名约定

#### A. 主播源流（客户端推）
```
stream_key = "{room_id}_{user_id}"
```
- 主播 alice 在房间 `room_1781448276492` 推流 → stream_key = `room_1781448276492_alice`
- 客户端拉流时也用这个 stream_key（拼上 `.flv`）

#### B. 翻译流（服务端翻译子进程推）— 客户端只需拉
```
stream_key = "translation_{source_user}_{to_lang}"
```
- 例：alice 配英→中翻译 → 翻译流 stream_key = `translation_alice_en`
- 客户端拉这个翻译流播给需要听翻译的用户

#### 命名约束
- `room_id` **必须以 `room` 开头**
- `stream_key` 里不能含 `/`

---

## 二、用户角色与状态

### 2.1 用户角色

| 角色 | 值 | 说明 | 权限 |
|------|------|------|------|
| 群主 | `owner` | 房间创建者 | 最高权限，可管理所有成员 |
| 普通成员 | `member` | 普通成员 | 基本发言权限 |
| 访客 | `guest` | 访客 | 受限权限 |

### 2.2 用户状态

| 状态 | 值 | 说明 |
|------|------|------|
| 正常 | `normal` | 可以正常发言 |
| 禁言 | `muted` | 不能发言 |
| 禁麦 | `mic_off` | 不能使用麦克风发布 |

---

## 三、房间管理接口

### 3.1 创建房间
- **路径**: `POST /api/v1/room`
- **描述**: 创建一个新房间，创建者自动成为群主

**请求体**:
```json
{
    "room_id": "room_123",
    "owner_id": "user_001",
    "name": "测试房间"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| room_id | string | 是 | 房间 ID，全局唯一，建议格式 `room_<13位时间戳>` |
| owner_id | string | 是 | 群主用户 ID |
| name | string | 否 | 房间名称（默认等于 room_id） |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "name": "测试房间",
        "owner_id": "user_001",
        "created_at": "2024-01-01 10:00:00",
        "max_members": 100,
        "allow_speak": true,
        "member_count": 1
    }
}
```

---

### 3.2 获取房间信息
- **路径**: `GET /api/v1/room/{room_id}`
- **描述**: 获取指定房间的详细信息

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "name": "测试房间",
        "owner_id": "user_001",
        "created_at": "2024-01-01 10:00:00",
        "max_members": 100,
        "allow_speak": true,
        "member_count": 5
    }
}
```

---

### 3.3 查询所有房间
- **路径**: `GET /api/v1/rooms`
- **描述**: 获取系统内所有房间的简表（用于房间列表页）

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "rooms": [
            { "room_id": "room_xxx", "owner_id": "alice", "member_count": 3 },
            { "room_id": "room_yyy", "owner_id": "bob", "member_count": 1 }
        ]
    }
}
```

---

### 3.4 删除房间
- **路径**: `DELETE /api/v1/room/{room_id}`
- **描述**: 删除指定房间（仅群主可操作）

**查询参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| operator_id | string | 否 | 操作者 ID；未传则为 `"system"` |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "deleted_members": ["user_001", "user_002"]
    }
}
```

**响应 409（有活跃资源）**:
```json
{
    "code": 409,
    "message": "房间内存在活跃资源，无法删除",
    "data": {
        "reasons": ["房间内存在拉流（2 个播放器）"],
        "active": {
            "has_publish": false,
            "has_play": true,
            "has_translation": false
        }
    }
}
```

---

## 四、成员管理接口

### 4.1 获取成员列表
- **路径**: `GET /api/v1/room/{room_id}/members`
- **描述**: 获取房间内所有成员详细信息（含 role / status / publish_allowed）

**查询参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| role | string | 否 | 按角色筛选（owner/member/guest） |
| status | string | 否 | 按状态筛选（normal/muted/mic_off） |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "member_count": 3,
        "allow_speak": true,
        "members": [
            {
                "user_id": "user_001",
                "role": "owner",
                "status": "normal",
                "publish_allowed": true,
                "joined_at": "2024-01-01 10:00:00",
                "last_active": "2024-01-01 10:30:00"
            },
            {
                "user_id": "user_003",
                "role": "member",
                "status": "muted",
                "publish_allowed": false,
                "joined_at": "2024-01-01 10:10:00"
            }
        ]
    }
}
```

> role 枚举：`owner` / `member` / `guest`
> status 枚举：`normal` / `muted`（被禁言） / `mic_off`（被禁麦）

---

### 4.2 获取成员详情
- **路径**: `GET /api/v1/room/{room_id}/member/{user_id}`
- **描述**: 获取指定成员的详细信息

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_id": "user_001",
        "room_id": "room_123",
        "role": "owner",
        "status": "normal",
        "publish_allowed": true,
        "joined_at": "2024-01-01 10:00:00",
        "last_active": "2024-01-01 10:30:00"
    }
}
```

---

### 4.3 加入房间
- **路径**: `POST /api/v1/room/{room_id}/join`
- **描述**: 用户加入指定房间。会广播 `member_joined` 事件给房间内所有 WS 客户端。

**请求体**:
```json
{
    "user_id": "user_002"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| user_id | string | 是 | 用户 ID |
| role | string | 否 | 用户角色（默认 member） |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_id": "user_002",
        "room_id": "room_123",
        "role": "member",
        "status": "normal",
        "publish_allowed": true,
        "joined_at": "2024-01-01 10:05:00"
    }
}
```

---

### 4.4 离开房间
- **路径**: `POST /api/v1/room/{room_id}/leave`
- **描述**: 用户主动离开房间。会广播 `member_left` 事件。

**请求体**:
```json
{
    "user_id": "user_002"
}
```

**响应 200**:
```json
{
    "code": 0,
    "message": "success"
}
```

---

### 4.5 踢出用户
- **路径**: `DELETE /api/v1/room/{room_id}/member/{user_id}/kick`
- **描述**: 将成员踢出房间

**查询参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| operator_id | string | 是 | 操作者 ID（群主） |

**权限说明**: 仅群主可踢人

**响应 200**:
```json
{
    "code": 0,
    "message": "success"
}
```

---

## 五、敲门接口

### 5.1 敲门请求
- **路径**: `POST /api/v1/room/{room_id}/knock`
- **描述**: 访客请求加入房间，通知房主

**请求体**:
```json
{
    "user_id": "visitor_001",
    "message": "你好，我想加入聊天"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| user_id | string | 是 | 敲门者用户 ID |
| message | string | 否 | 留言信息 |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "owner_id": "user_001",
        "knocker_id": "visitor_001"
    }
}
```

---

### 5.2 接受敲门
- **路径**: `POST /api/v1/room/{room_id}/knock/accept`
- **描述**: 房主接受访客的敲门请求，自动将访客加入房间

**请求体**:
```json
{
    "operator_id": "user_001",
    "knocker_id": "visitor_001",
    "role": "member"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| operator_id | string | 是 | 操作者 ID（房主） |
| knocker_id | string | 是 | 被接受的敲门者 ID |
| role | string | 否 | 分配的角色（默认 member） |

**权限**: 仅房主可操作

**响应 200**:
```json
{
    "code": 0,
    "message": "success"
}
```

**事件通知**:
- 敲门者收到 `room_knock_accepted` 事件
- 房间内其他成员收到 `member_joined` 事件

---

### 5.3 拒绝敲门
- **路径**: `POST /api/v1/room/{room_id}/knock/reject`
- **描述**: 房主拒绝访客的敲门请求

**请求体**:
```json
{
    "operator_id": "user_001",
    "knocker_id": "visitor_001",
    "reason": "房间已满"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| operator_id | string | 是 | 操作者 ID（房主） |
| knocker_id | string | 是 | 被拒绝的敲门者 ID |
| reason | string | 否 | 拒绝原因 |

**权限**: 仅房主可操作

**响应 200**:
```json
{
    "code": 0,
    "message": "success"
}
```

**事件通知**:
- 敲门者收到 `room_knock_rejected` 事件

---

## 六、禁言/禁麦接口

### 6.1 禁言用户
- **路径**: `POST /api/v1/room/{room_id}/member/{user_id}/mute`
- **描述**: 禁言指定用户（禁止发言）

**请求体**:
```json
{
    "operator_id": "user_001"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| operator_id | string | 是 | 操作者 ID（群主） |

**注意**: 群主不能被禁言。

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_id": "user_002",
        "status": "muted",
        "publish_allowed": false
    }
}
```

---

### 6.2 解除禁言
- **路径**: `POST /api/v1/room/{room_id}/member/{user_id}/unmute`
- **描述**: 解除指定用户的禁言状态

**请求体**:
```json
{
    "operator_id": "user_001"
}
```

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_id": "user_002",
        "status": "normal",
        "publish_allowed": true
    }
}
```

---

### 6.3 禁麦（禁止麦克风）
- **路径**: `POST /api/v1/room/{room_id}/member/{user_id}/mic/disable`
- **描述**: 禁止指定用户使用麦克风发布音频

**请求体**:
```json
{
    "operator_id": "user_001"
}
```

**注意**: 群主不能被禁麦。

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_id": "user_002",
        "status": "mic_off",
        "publish_allowed": false
    }
}
```

---

### 6.4 解除禁麦
- **路径**: `POST /api/v1/room/{room_id}/member/{user_id}/mic/enable`
- **描述**: 解除指定用户的禁麦状态

**请求体**:
```json
{
    "operator_id": "user_001"
}
```

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_id": "user_002",
        "status": "normal",
        "publish_allowed": true
    }
}
```

---

### 6.5 全体禁言
- **路径**: `POST /api/v1/room/{room_id}/mute-all`
- **描述**: 开启全体禁言（除群主外所有人禁止发言）

**请求体**:
```json
{
    "operator_id": "user_001"
}
```

**注意**: 只有群主可以执行全体禁言。

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "allow_speak": false,
        "muted_count": 5
    }
}
```

---

### 6.6 解除全体禁言
- **路径**: `POST /api/v1/room/{room_id}/unmute-all`
- **描述**: 关闭全体禁言

**请求体**:
```json
{
    "operator_id": "user_001"
}
```

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "allow_speak": true,
        "unmuted_count": 5
    }
}
```

---

### 6.7 检查发言权限
- **路径**: `GET /api/v1/room/{room_id}/check-publish?user_id=xxx`
- **描述**: 检查用户是否可以发布（发言）。在客户端请求推流前调用。

**查询参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| user_id | string | 是 | 用户 ID |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "user_id": "user_001",
        "can_publish": true,
        "status": "normal"
    }
}
```

**客户端推荐流程**:
```
用户点 "上麦"
  → 调 /check-publish?user_id=xxx
  → true: 开始推 RTMP 流
  → false: toast "您当前没有发言权限，请联系房主"
```

---

## 七、用户说话状态

### 7.1 获取正在说话的用户
- **路径**: `GET /api/v1/room/{room_id}/speaking`
- **描述**: 获取房间中正在说话的用户列表

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "room_id": "room_123",
        "speaking_users": ["user_001", "user_002"]
    }
}
```

### 7.2 用户开始说话（WebSocket 事件）

当用户开始推流（发言）时，服务端通过 WebSocket 广播 `user_speaking_start` 事件给房间所有人。

```json
{
    "type": "user_speaking_start",
    "room_id": "room_001",
    "user_id": "user_A",
    "data": {
        "stream_url": "rtmp://localhost/live/room_001_user_A"
    },
    "timestamp": "2026-04-27 10:25:00"
}
```

### 7.3 用户停止说话（WebSocket 事件）

当用户停止推流时，服务端通过 WebSocket 广播 `user_speaking_stop` 事件给房间所有人。

```json
{
    "type": "user_speaking_stop",
    "room_id": "room_001",
    "user_id": "user_A",
    "data": {},
    "timestamp": "2026-04-27 10:26:00"
}
```

---

## 八、健康检查

### 8.1 健康检查
- **路径**: `GET /api/v1/health`
- **描述**: 探活 / 健康检查，无需鉴权

**响应 200**:
```json
{ "status": "ok" }
```

> 同样存在不带 `/api/v1` 前缀的 `GET /health`（历史遗留），两个等价，建议统一用 `/api/v1/health`。

---

## 九、翻译功能

### 9.1 翻译流程

```
1. 主播推 RTMP 到 SRS（源流 stream_key = "{room_id}_{source_user}"）
2. 客户端调 POST /translation/start → 服务端起翻译子进程
3. 客户端通过 WS 收到 translation_started 通知
4. 客户端每 5 秒调 POST /translation/heartbeat 保活
5. 客户端通过 WS 收到 translation_text（字幕内容）
6. 客户端拉翻译流给需要听翻译的用户
7. 客户端调 POST /translation/stop 停止
```

### 9.2 启动翻译
- **路径**: `POST /api/v1/translation/start`

**请求体**:
```json
{
    "room_id": "room_123",
    "source_user": "user_001",
    "target_user": "user_002",
    "source_lang": "zh",
    "target_lang": "en"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| room_id | string | 是 | 房间 ID |
| source_user | string | 是 | 谁的语言要被翻译 |
| target_user | string | 是 | 谁要听翻译结果 |
| source_lang | string | 否 | 源语言；不传则为 `"auto"`（自动检测） |
| target_lang | string | 否 | 目标语言 |
| to_lang | string | 否 | 兼容字段；当 target_lang 为空时使用 |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "request_id": "trans_1781744193000"
    }
}
```

> ⚠️ 同一 (room_id, source_user, target_lang) 重复 start，服务端会先杀掉旧翻译再启新的。

### 9.3 翻译心跳
- **路径**: `POST /api/v1/translation/heartbeat`
- **描述**: 告诉服务端"我还活着"。服务端 30 秒没收到心跳会主动 stop 翻译子进程。

**频率**: 建议每 5 秒一次。

**请求体**:
```json
{
    "room_id": "room_123",
    "source_user": "user_001",
    "client_id": "session_xxx",
    "to_lang": "en",
    "request_id": "trans_1781744193000"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| room_id | string | 是 | 房间 ID |
| source_user | string | 是 | 同 /start |
| client_id | string | 是 | 客户端唯一标识 |
| to_lang | string | 否 | 同 /start 的 target_lang |
| request_id | string | 否 | 有则精确匹配 |

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "updated": true
    }
}
```

`updated: false` 表示服务端没找到对应的活跃翻译（可能已 stop），不是错误。

### 9.4 停止翻译
- **路径**: `POST /api/v1/translation/stop?request_id=xxx`
- **描述**: 客户端主动停止一个翻译。会通过 WS 广播 `translation_stopped` 事件。

**查询参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| request_id | string | 是 | /start 返回的 ID |

> 所有参数走 query string，无 Body。

**响应 200**:
```json
{
    "code": 0,
    "message": "success"
}
```

### 9.5 查询活跃翻译
- **路径**: `GET /api/v1/translation/active`
- **描述**: 调试时查看当前所有活跃翻译。一般客户端不需要调。

**响应 200**:
```json
{
    "code": 0,
    "message": "success",
    "data": {
        "translations": [
            {
                "request_id": "trans_xxx",
                "room_id": "room_xxx",
                "source_user": "user_001",
                "target_user": "user_002",
                "to_lang": "en",
                "source_lang": "zh",
                "status": "active"
            }
        ]
    }
}
```

---

## 十、WebSocket 实时通知

### 10.1 连接方式

```
WS  ws://<server>:8085/ws?room={room_id}&user={user_id}
```

| 参数 | 必填 | 说明 |
|------|------|------|
| room | 是 | 房间 ID |
| user | 否 | 用户 ID；不传仍能连接，但无法定向推送 |

**连接成功响应**:
```json
{ "type": "connected", "room_id": "room_xxx", "user_id": "user_001" }
```

### 10.2 客户端 → 服务端消息

| type | 用途 | 响应 |
|------|------|------|
| `ping` | 心跳 | 服务端回 `{"type":"pong"}` |
| `subscribe` | 切换房间（保持 ws 连接，仅换房间） | 服务端回 `{"type":"connected","room_id":"<新房间>"}` |

### 10.3 服务端 → 客户端事件

| type | 触发时机 | 说明 |
|------|---------|------|
| `connected` | 连接成功 | 含 room_id, user_id |
| `pong` | 客户端发 ping 后 | — |
| `member_joined` | 有人调 /join | 含 room_id, user_id |
| `member_left` | 有人调 /leave | 含 room_id, user_id |
| `member_kicked` | 用户被踢出 | 含 room_id, user_id |
| `member_role_changed` | 用户角色变更 | 含 user_id, data.old_role, data.new_role |
| `member_muted` | 用户被禁言 | 含 user_id, operator_id |
| `member_unmuted` | 用户被解除禁言 | 含 user_id, operator_id |
| `member_mic_disabled` | 用户被禁麦 | 含 user_id, operator_id |
| `member_mic_enabled` | 用户被解除禁麦 | 含 user_id, operator_id |
| `room_muted_all` | 全体禁言开启 | 含 data.muted_count |
| `room_unmuted_all` | 全体禁言关闭 | 含 data.unmuted_count |
| `room_created` | 房间创建 | 含房间完整信息 |
| `room_deleted` | 房间被删除 | 含 room_id, message |
| `room_knock` | 有人敲门（仅房主收到） | 含 knocker_id, data.message |
| `room_knock_accepted` | 敲门被接受 | 含 room_id |
| `room_knock_rejected` | 敲门被拒绝 | 含 data.reason |
| `translation_started` | 翻译启动 | 含 room_id, source_user, target_user, target_lang |
| `translation_text` | 翻译子进程产出文本 | 含 original_text, translated_text, source_lang, target_lang |
| `translation_stopped` | 翻译停止 | 含 room_id, source_user |
| `original_speech_text` | 原文文本推送 | 含 original_text, source_lang，房间所有人均收到 |
| `user_speaking_start` | 用户开始说话 | 含 stream_url |
| `user_speaking_stop` | 用户停止说话 | — |

### 10.4 事件消息格式

```json
{
    "type": "member_muted",
    "room_id": "room_123",
    "user_id": "user_001",
    "operator_id": "owner_001",
    "data": {},
    "timestamp": "2024-01-01 10:30:00"
}
```

### 10.5 room_deleted 注意事项

服务端会广播两次 `room_deleted`：
1. **第一次**（通过 `broadcast_to_room`）— 包含 `deleted_by` 字段
2. **第二次**（关闭 ws 前）— 不包含 `deleted_by` 字段

客户端必须只信 `type`，**不要依赖 `deleted_by` 是否存在**。

### 10.6 翻译事件消息格式

#### original_speech_text（原文推送 — 房间所有人）

> **关键**：无论是否有人申请翻译，只要翻译服务在运行，原文都会广播给房间所有在线用户。

```json
{
    "type": "original_speech_text",
    "room_id": "room_001",
    "user_id": "user_A",
    "data": {
        "original_text": "Hello everyone, welcome to the meeting",
        "source_lang": "en"
    },
    "timestamp": "2026-04-27 10:25:30"
}
```

#### translation_text（翻译文本 — 目标用户）

> **关键**：翻译文本只推送给申请翻译的目标用户。

```json
{
    "type": "translation_text",
    "room_id": "room_001",
    "user_id": "user_A",
    "target_user_id": "user_B",
    "data": {
        "original_text": "Hello everyone, welcome to the meeting",
        "translated_text": "大家好，欢迎参加本次会议",
        "source_lang": "en",
        "target_lang": "zh"
    },
    "timestamp": "2026-04-27 10:25:31"
}
```

#### translation_started / translation_stopped

```json
{
    "type": "translation_started",
    "room_id": "room_001",
    "user_id": "user_A",
    "target_user_id": "user_B",
    "data": {
        "to_lang": "zh"
    },
    "timestamp": "2026-04-27 10:25:00"
}
```

---

## 十一、错误码说明

| code | 说明 | 客户端处理 |
|------|------|-----------|
| 0 | 成功 | 正常处理 |
| 400 | 请求参数错误 | 弹 toast 提示，不重试 |
| 403 | 权限不足 | 提示无权限 |
| 404 | 资源不存在 | 兜底清理本地状态 |
| 409 | 资源冲突/活跃资源冲突 | 弹 reasons 详情，不自动重试 |
| 422 | 参数校验失败 | 检查请求格式 |
| 500 | 服务器内部错误 | toast + 可重试 |

### 11.1 错误响应格式

```json
{
    "code": 400,
    "message": "错误信息"
}
```

**特例** — 409 房间删除冲突时 detail 为对象:
```json
{
    "code": 409,
    "message": "房间内存在活跃资源，无法删除",
    "data": {
        "reasons": ["..."],
        "active": {}
    }
}
```

---

## 十二、权限规则汇总

| 操作 | 群主 | 普通成员 |
|------|:----:|:--------:|
| 禁言/禁麦成员 | ✅ | ❌ |
| 解除禁言/禁麦 | ✅ | ❌ |
| 全体禁言 | ✅ | ❌ |
| 踢人 | ✅ | ❌ |
| 删除房间 | ✅ | ❌ |

**重要限制**: 群主不能被禁言、禁麦或踢出

---

## 十三、接口速查表

| 分类 | 接口 | 方法 | 章节 |
|------|------|------|------|
| 健康 | `/api/v1/health` | GET | §8.1 |
| 房间 | `/api/v1/room` | POST | §3.1 创建 |
| 房间 | `/api/v1/room/{room_id}` | GET | §3.2 详情 |
| 房间 | `/api/v1/rooms` | GET | §3.3 全部房间 |
| 房间 | `/api/v1/room/{room_id}` | DELETE | §3.4 删除 |
| 房间 | `/api/v1/room/{room_id}/join` | POST | §4.3 加入 |
| 房间 | `/api/v1/room/{room_id}/leave` | POST | §4.4 离开 |
| 成员 | `/api/v1/room/{room_id}/members` | GET | §4.1 列表 |
| 成员 | `/api/v1/room/{room_id}/member/{user_id}` | GET | §4.2 详情 |
| 成员 | `/api/v1/room/{room_id}/member/{user_id}/kick` | DELETE | §4.5 踢出 |
| 敲门 | `/api/v1/room/{room_id}/knock` | POST | §5.1 敲门 |
| 敲门 | `/api/v1/room/{room_id}/knock/accept` | POST | §5.2 接受 |
| 敲门 | `/api/v1/room/{room_id}/knock/reject` | POST | §5.3 拒绝 |
| 禁言 | `/api/v1/room/{room_id}/member/{user_id}/mute` | POST | §6.1 禁言 |
| 禁言 | `/api/v1/room/{room_id}/member/{user_id}/unmute` | POST | §6.2 解除禁言 |
| 禁言 | `/api/v1/room/{room_id}/member/{user_id}/mic/disable` | POST | §6.3 禁麦 |
| 禁言 | `/api/v1/room/{room_id}/member/{user_id}/mic/enable` | POST | §6.4 解除禁麦 |
| 禁言 | `/api/v1/room/{room_id}/mute-all` | POST | §6.5 全体禁言 |
| 禁言 | `/api/v1/room/{room_id}/unmute-all` | POST | §6.6 解除全体禁言 |
| 禁言 | `/api/v1/room/{room_id}/check-publish` | GET | §6.7 检查发言权限 |
| 说话 | `/api/v1/room/{room_id}/speaking` | GET | §7.1 |
| 翻译 | `/api/v1/translation/start` | POST | §9.2 |
| 翻译 | `/api/v1/translation/heartbeat` | POST | §9.3 |
| 翻译 | `/api/v1/translation/stop` | POST | §9.4 |
| 翻译 | `/api/v1/translation/active` | GET | §9.5 |
| WS | `/ws?room=&user=` | GET(WS) | §10 |
| SRS 推流 | `rtmp://<server>:1935/live/{stream_key}` | RTMP | §1.1 |
| SRS 拉流 | `http://<server>:8080/live/{stream_key}.flv` | HTTP-FLV | §1.1 |

---

## 十四、变更日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-06-19 | 1.0 | 首次发布：综合 `客户端API.md` 与 `语音聊天室-用户管理接口文档.md`，移除"更新成员角色"接口，统一响应格式 |
